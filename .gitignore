# Секреты и локальные конфиги — никогда не коммитим
.env
*.env
.hermes/

# Мусор ОС/редакторов
.DS_Store

# На случай, если сюда когда-нибудь въедет код
node_modules/
data/

# Питон-мусор (плагин project_index)
__pycache__/
*.pyc

# hermes-web: локальная SQLite-база (только на сервере/в тестах)
hermes-web/*.db
hermes-web/**/*.db

# Рабочие артефакты Subagent-Driven Development (брифы, отчёты, дифф-пакеты
# для ревью) — только для текущей сессии, не часть истории проекта
.superpowers/sdd/
