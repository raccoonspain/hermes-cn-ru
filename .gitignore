# Секреты и локальные конфиги — никогда не коммитим
.env
*.env
.hermes/

# Мусор ОС/редакторов
.DS_Store

# На случай, если сюда когда-нибудь въедет код
node_modules/
data/

# Питон-мусор (плагин project_index)
__pycache__/
*.pyc

# Git worktree'ы для изолированной разработки фич (superpowers:using-git-worktrees) —
# каждый содержит свой .git, не часть истории основного репозитория
.claude/worktrees/
