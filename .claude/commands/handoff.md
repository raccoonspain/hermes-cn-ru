# 🤝 ПЕРЕДАЧА ПРОЕКТА — ЧИТАЙ ЭТО ПЕРВЫМ

> Этот файл — точка входа для **новой нейронки** (или человека), которая
> впервые открывает проект. Цель: за 5 минут понять **что это, где мы
> сейчас и куда идём** — без необходимости читать весь код.

---

## Как устроена память проекта

Весь контекст проекта живёт в четырёх файлах. У каждого — одна роль.
Читай их **в этом порядке**:

| # | Файл | Отвечает на вопрос | Как меняется |
|---|------|--------------------|--------------|
| 1 | [project-brief.md](./project-brief.md) | **Что** это за проект и **зачем** | Редко (меняется суть проекта) |
| 2 | [state.md](./state.md) | **Где мы сейчас** и **что делать дальше** | Постоянно — это «живой снимок» |
| 3 | [changelog.md](./changelog.md) | **Что уже сделали** (по датам) | Только дописывается вниз |
| 4 | [decisions.md](./decisions.md) | **Почему** так, а не иначе | Только дописывается вниз |

Плюс [../CLAUDE.md](../CLAUDE.md) — инструкции по стеку, деплою и правилам работы.

**Правило, которое держит всё это в актуальном состоянии:**
после каждого осмысленного шага мы обновляем `state.md` + дописываем
`changelog.md` и **коммитим в git**. Поэтому история проекта = история
коммитов, а текущее положение дел всегда в `state.md`. Подробнее — в
разделе «ЖУРНАЛ ПРОЕКТА И КОММИТЫ» в `CLAUDE.md`.

---

## Быстрый старт для входящей нейронки

1. Прочитай 4 файла выше по порядку.
2. Глянь последние коммиты — это лента того, что реально делалось:
   ```bash
   git log --oneline -20
   ```
3. Если нужно увидеть, что менялось в конкретном шаге:
   ```bash
   git show <hash>
   ```
4. Запусти проект, убедись что он живой:
   ```bash
   npm install && npm run check
   ```
5. Возьми первый пункт из раздела «Следующие шаги» в `state.md` — и продолжай.

---

## Снимок передачи

> Перед тем как передать проект дальше, обнови блок ниже (или вызови `/handoff`).
> Это TL;DR на одну страницу — самое важное, что нельзя потерять.

**Дата снимка:** 2026-07-29 (пользователь попросил подготовить снимок и
отошёл — сессию можно продолжить прямо с этого места без потери контекста)

**В одном предложении:** MVP веб-морды Hermes (авторизация, «Быстрый
чат», навигация по Группам/Проектам, рабочий экран проекта) реализован,
живёт на `hermes.blackboxbegin.space`, прошёл несколько раундов живых
прогонов и фиксов (D-011…D-013); последний живой прогон в тот же день,
что был выкачен D-013, показал: проблема «модель кладёт файлы мимо
`result/` и не спрашивает пользователя» **не решена**, только
продиагностирована — черновик спека написан и ждёт вашего ревью, это
приоритет №1 по возвращении.

**Что работает прямо сейчас:** вход по логину/паролю, «Быстрый чат»,
`project-selector.html` (Группы/Проекты, RAG-поиск, перенос между
группами), `project-workspace.html` (дерево `source/outer/result`,
теперь с видимыми пустыми папками — D-013, чтение/сохранение
`.md`/`.txt`, загрузка вложений, селектор «папка для результата»
рядом с «папка для вложений», чат по конкретному проекту, живая
«Активность агента» поверх SSE), автоматическая самопочинка владельца
файлов workspace (D-012). Полный список фиксов по датам — `changelog.md`.

**Что в процессе / недоделано — ТОЧКА ОСТАНОВКИ, читать в первую очередь:**
черновик спека
`docs/superpowers/specs/2026-07-29-result-placement-reliability-and-collapsible-tree-design.md`
написан по итогам живого расследования (не согласован построчно — это
рекомендация, не решение). Реализация **ещё не начата**, worktree/план
ещё не создавались. Две независимые части:

- **Часть A — почему D-013 не сработало.** Прямой запрос к Hermes API
  (`GET /api/sessions/{id}/messages`) показал: модель не колебалась и не
  спрашивала — она уверенно продолжила писать в папку `3-kirik-3-23-29`
  в корне проекта, потому что делала так уже много ходов подряд (часть
  файлов в этом же проекте датирована на 5 дней раньше, чем в проекте
  вообще появился `source/outer/result`). Устоявшийся в истории долгого
  диалога прецедент перевешивает один абзац усиленного системного
  сообщения на конкретный ход — системное ограничение чисто
  промпт-based решения, не баг реализации D-013. Предложено (спек,
  «Часть A», можно внедрять частично — **открытый вопрос вам**, что из
  этого делать): (A1) логировать `result_target`/факт усиления на
  каждый ход — сейчас никакой похожий случай нельзя доказательно
  разобрать постфактум; (A2) кнопка «→ переместить в result/» на
  записях в разделе «остальное» дерева — надёжный ручной фолбэк, не
  зависящий от послушания модели; (A3) ещё раз усилить формулировку
  промпта — дёшево, но эффект недоказуем на фоне находки выше.
- **Часть B — сворачиваемые вложенные папки в дереве файлов** (второй,
  независимый запрос из того же сообщения — тот же `misc`-раздел уже
  разросся до дюжин записей). Дизайн: треугольник-тоггл на каждой
  папке, состояние — в `localStorage` по проекту (сразу персистентно, в
  отличие от прежнего клиентского костыля из D-013), дефолт — всё
  развёрнуто. **Открытый вопрос вам**: сворачивать ли `misc` по
  умолчанию при большом числе записей, или всегда стартовать
  развёрнутым (в спеке — рекомендация «нет, всегда развёрнутым»).

**Следующий шаг по возвращении:** прочитать спек, ответить на два
открытых вопроса внутри (прямо в чате или новыми решениями в
`decisions.md`), затем `writing-plans` → `executing-plans`/
`subagent-driven-development`, как в предыдущих срезах.

После этой находки — под-проект C (админ-панель, метрики VPS/wormsoft.ru).

**Где живёт прод (URL / SERVER_ID):** `https://hermes.blackboxbegin.space`
→ VPS `212.115.55.116` (`hermes-web.service` на `127.0.0.1:8643` за
Caddy, `hermes-gateway.service` — сам агент, порт `127.0.0.1:8642`).
SSH-ключ: `~/.ssh/id_ed25519_hermes_user`, пользователь `hermes`.

**Главные подводные камни:**
- `/workspace/...` внутри Docker-песочницы агента — алиас, который
  понимает только shell (`terminal`/`execute_code`); инструменты
  `write_file`/`read_file` — не-докеризованные и всегда отклонят
  абсолютный `/workspace/...` путь. Это вендорный код Hermes
  (`agent/file_safety.py`), чинится только промптом (system-сообщение в
  `quickchat.py`), не гарантированно — модель может всё равно ошибиться.
  **Это же ограничение — первопричина текущей точки остановки**: промпт
  можно усилить, но нельзя гарантировать, особенно против устоявшегося
  прецедента в истории того же диалога (см. выше).
- `approvals.mode: manual` (D-004) не имеет для веб-чата штатной
  подсказки про `/approve` — открытый вопрос, см. `docs/state.md`
  «Открытые вопросы / блокеры».
- Диагностика живых багов эффективнее всего через прямой запрос к
  Hermes API (`GET http://127.0.0.1:8642/api/sessions/{id}/messages` на
  сервере с `Authorization: Bearer $API_SERVER_KEY` из `~/.hermes/.env`)
  — сырая история сессии, а не только `journalctl`. Именно так была
  найдена причина текущей точки остановки. **Слепая зона**: `hermes-web`
  не логирует ни тело входящего HTTP-запроса, ни собранное системное
  сообщение — отсюда и предложение A1 в спеке.
- Тестовый venv для `hermes-web`/`project_index` живёт вне репозитория,
  в scratchpad-директории конкретной сессии
  (`/tmp/claude-1000/.../scratchpad/hermes-web-venv`) — если сессия
  сброшена, создать заново по образцу плана 2026-07-26 (aiohttp/argon2-
  cffi/pytest/pytest-asyncio, версии — см. тот план).

**Первое, что стоит сделать новому исполнителю:** прочитать
`docs/superpowers/specs/2026-07-29-result-placement-reliability-and-collapsible-tree-design.md`
целиком, обсудить с пользователем два открытых вопроса внутри, затем
писать план реализации (TDD, по образцу предыдущих срезов).
