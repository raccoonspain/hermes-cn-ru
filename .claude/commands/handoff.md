# 🤝 ПЕРЕДАЧА ПРОЕКТА — ЧИТАЙ ЭТО ПЕРВЫМ

> Этот файл — точка входа для **новой нейронки** (или человека), которая
> впервые открывает проект. Цель: за 5 минут понять **что это, где мы
> сейчас и куда идём** — без необходимости читать весь код.

---

## Как устроена память проекта

Весь контекст проекта живёт в четырёх файлах. У каждого — одна роль.
Читай их **в этом порядке**:

| # | Файл | Отвечает на вопрос | Как меняется |
|---|------|--------------------|--------------|
| 1 | [project-brief.md](./project-brief.md) | **Что** это за проект и **зачем** | Редко (меняется суть проекта) |
| 2 | [state.md](./state.md) | **Где мы сейчас** и **что делать дальше** | Постоянно — это «живой снимок» |
| 3 | [changelog.md](./changelog.md) | **Что уже сделали** (по датам) | Только дописывается вниз |
| 4 | [decisions.md](./decisions.md) | **Почему** так, а не иначе | Только дописывается вниз |

Плюс [../CLAUDE.md](../CLAUDE.md) — инструкции по стеку, деплою и правилам работы.

**Правило, которое держит всё это в актуальном состоянии:**
после каждого осмысленного шага мы обновляем `state.md` + дописываем
`changelog.md` и **коммитим в git**. Поэтому история проекта = история
коммитов, а текущее положение дел всегда в `state.md`. Подробнее — в
разделе «ЖУРНАЛ ПРОЕКТА И КОММИТЫ» в `CLAUDE.md`.

---

## Быстрый старт для входящей нейронки

1. Прочитай 4 файла выше по порядку.
2. Глянь последние коммиты — это лента того, что реально делалось:
   ```bash
   git log --oneline -20
   ```
3. Если нужно увидеть, что менялось в конкретном шаге:
   ```bash
   git show <hash>
   ```
4. Запусти проект, убедись что он живой:
   ```bash
   npm install && npm run check
   ```
5. Возьми первый пункт из раздела «Следующие шаги» в `state.md` — и продолжай.

---

## Снимок передачи

> Перед тем как передать проект дальше, обнови блок ниже (или вызови `/handoff`).
> Это TL;DR на одну страницу — самое важное, что нельзя потерять.

**Дата снимка:** 2026-08-01 (сразу после брейнсторма и спека Группы D —
пользователь взял паузу, сессию можно продолжить прямо с `writing-plans`
по готовому спеку, без потери контекста)

**В одном предложении:** MVP веб-морды Hermes реализован и живёт на
`hermes.blackboxbegin.space` (D-011…D-014, 2026-07-29); следующий за MVP
бэклог живого тестирования — **Группы B, A, C закрыты целиком**, все три
задеплоены на VPS и подтверждены живой приёмкой через реальный HTTP API
(не только UI); **Группа D** (последняя в бэклоге — layout-редизайн:
модальный редактор файлов → раздвижная панель между деревом и чатом)
прошла брейнсторм и получила согласованный спек **2026-08-01** — план
(`writing-plans`) ещё не написан, это следующий шаг.

**Что работает прямо сейчас:** вход по логину/паролю, «Быстрый чат»
(теперь ведёт сразу на полноценный `project-workspace.html`, старый
`chat.html` удалён — Группа C), `project-selector.html` (Группы/Проекты,
RAG-поиск, перенос между группами, **удаление проекта** в `.trash/`,
**создание проекта** прямо из списка в выбранную группу, **редактирование
тегов/статуса** как структурированные поля с автокомплитом — Группа A),
`project-workspace.html` — дерево `source/outer/result` + «остальное»
(`misc`), сворачиваемое по умолчанию (D-014), чтение/сохранение `.md`/
`.txt` в любом месте проекта, кнопка «→ переместить» на любом файле/папке,
селектор «папка для результата», загрузка вложений, живая «Активность
агента» поверх SSE, автоматическая самопочинка владельца файлов
workspace. Полный список фиксов по датам — `changelog.md`.

**Известный, сознательно отложенный минус (не блокирует, лёгкий фикс на
будущее):** `hermes_web/run.py`'s `configure_logging()` отключает
`propagate` у логгера `aiohttp.access`, чтобы не задваивать access-строки
— но на живом сервере подтверждено, что это **полностью глушит
access-лог** в journald, а не просто убирает дубли. Исправление: убрать
строку `logging.getLogger("aiohttp.access").propagate = False` в
`run.py` — по мнению ревьюера, встроенная проверка
`aiohttp.web.run_app` (`hasHandlers()`) и так не даст задвоить строки.

**Группа D — спек готов, план ещё нет.** Модальный редактор файлов
заменяется раздвижной панелью между деревом и чатом (панель раздвигает
layout, чат сужается, но остаётся рабочим — не «редактор разворачивается
на весь центр», это отклонённый вариант). Решение по варианту раскладки
зафиксировано интерактивным HTML-черновиком (артефакт, два варианта на
реалистичных данных) — пользователь попробовал оба вживую и выбрал этот.
Раскладка меняется с CSS Grid на Flexbox. Все детали поведения (сохранение
не закрывает панель, confirm() на переключение файла с несохранёнными
правками, глобальная — не per-проектная — персистентность ширин колонок
в localStorage, что сознательно НЕ делается — номера строк, вкладки
файлов, адаптивность) — в спеке:
`docs/superpowers/specs/2026-08-01-workspace-layout-redesign-design.md`.
**Следующий шаг: `writing-plans` по этому спеку**, открытых
дизайн-вопросов не осталось.

*(Отдельно от Группы D: есть более старая, никогда не начатая идея
пользователя — редактирование скилов/команд Hermes в рамках проекта,
объём которой сам пользователь пока не уточнил. Не путать с Группой D —
это другая, более гипотетическая задача на будущее, см.
`docs/superpowers/specs/2026-07-29-result-placement-reliability-and-collapsible-tree-design.md`,
раздел «Часть D». Требует отдельного брейнсторминга, когда/если
пользователь сузит объём.)*

После Группы D — под-проект C (админ-панель, метрики VPS/wormsoft.ru,
дизайн уже согласован в D-009/макетах).

**Где живёт прод (URL / SERVER_ID):** `https://hermes.blackboxbegin.space`
→ VPS `212.115.55.116` (`hermes-web.service` на `127.0.0.1:8643` за
Caddy, `hermes-gateway.service` — сам агент, порт `127.0.0.1:8642`).
SSH-ключ: `~/.ssh/id_ed25519_hermes_user`, пользователь `hermes`.

**Главные подводные камни:**
- `/workspace/...` внутри Docker-песочницы агента — алиас, который
  понимает только shell (`terminal`/`execute_code`); инструменты
  `write_file`/`read_file` — не-докеризованные и всегда отклонят
  абсолютный `/workspace/...` путь. Это вендорный код Hermes
  (`agent/file_safety.py`), чинится только промптом (system-сообщение в
  `quickchat.py`), не гарантированно — модель может всё равно ошибиться.
  **Это же ограничение — первопричина всей истории D-013/D-014**: промпт
  можно усилить, но нельзя гарантировать, особенно против устоявшегося
  прецедента в истории того же диалога. D-014 явно принял это как
  системный предел и вместо дальнейшего усиления промпта (A3, сознательно
  НЕ сделан) добавил надёжный ручной фолбэк (A2 — кнопка «переместить»)
  и диагностику (A1 — логирование).
- `approvals.mode: manual` (D-004) не имеет для веб-чата штатной
  подсказки про `/approve` — открытый вопрос, см. `docs/state.md`
  «Открытые вопросы / блокеры».
- Диагностика живых багов эффективнее всего через прямой запрос к
  Hermes API (`GET http://127.0.0.1:8642/api/sessions/{id}/messages` на
  сервере с `Authorization: Bearer $API_SERVER_KEY` из `~/.hermes/.env`)
  — сырая история сессии, а не только `journalctl`. Именно так была
  найдена первопричина проблемы, закрытой в D-014.
- Тестовый venv для `hermes-web`/`project_index` живёт вне репозитория,
  в scratchpad-директории конкретной сессии
  (`/tmp/claude-1000/.../scratchpad/hermes-web-venv`) — если сессия
  сброшена, создать заново по образцу плана 2026-07-26 (aiohttp/argon2-
  cffi/pytest/pytest-asyncio, версии — см. тот план).
- На сервере `hermes-web` — НЕ git-чекаут, а просто набор файлов,
  разложенных `rsync`'ом (`/home/hermes/hermes-web`). Деплой = `rsync`
  каждого изменённого файла + `systemctl --user restart
  hermes-web.service`, не `git pull`. Одноразовые тестовые аккаунты
  (`qa_temp`) создаются вручную через `hermes_web.storage.create_user` +
  `auth.hash_password` (см. `hermes_web/seed_users.py`, но его CLI требует
  интерактивный `getpass` — проще инлайновым Python-вызовом по SSH) и
  обязательно удаляются после проверки, вместе с тестовым проектом в
  `~/workspace/qa_temp`.

**Первое, что стоит сделать новому исполнителю:** взять готовый спек
`docs/superpowers/specs/2026-08-01-workspace-layout-redesign-design.md`
и вызвать `writing-plans` — открытых дизайн-вопросов по Группе D не
осталось, брейнсторм уже проведён и согласован. После плана — как
обычно, `subagent-driven-development`. После Группы D — под-проект C
(админ-панель, метрики VPS/wormsoft.ru, дизайн уже согласован в
D-009/макетах).
