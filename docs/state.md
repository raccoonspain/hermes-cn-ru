# 📍 Состояние проекта — где мы сейчас

> **Живой снимок.** Этот файл всегда отражает текущее положение дел.
> Его не дописывают вниз, а **перезаписывают** — здесь только «сейчас»,
> вся история — в [changelog.md](./changelog.md).
> Обновляй после каждого осмысленного шага и **коммить**.

**Последнее обновление:** 2026-08-02
**Фаза:** Под-проект A (Hermes-плагин `project_index`) и все три среза под-проекта B (срез 1+2 — авторизация + «Быстрый чат»; срез 3 часть 1 — навигация по Группам/Проектам; срез 3 часть 2 — полноценный `project-workspace.html`) реализованы, влиты в `main` и выкачены на VPS — с приёмочными тестами на реальном сервере и финальным ревью всей ветки на каждый срез (срез 1+2: 1 критичная + 6 важных; срез 3 часть 1: 1 критичная — path traversal в переносе проектов, реально проверенная эксплуатацией — + 3 важные; срез 3 часть 2: 1 критичная — симлинк-эскейп из песочницы агента через загрузку вложений, была живой на проде — + 6 важных, все закрыты фикс-проходом и передеплоены). Публичный домен `hermes.blackboxbegin.space` живой: вход, «Быстрый чат», навигация по Группам/Проектам, полноценный рабочий экран проекта (дерево `source/outer/result` с подпапками, чтение/сохранение текстовых файлов, загрузка вложений, живая «Активность агента» поверх SSE) — **MVP веб-морды под-проекта B закрыт целиком**. Первый живой прогон реального пользовательского сценария (физика, PDF-задачник) вскрыл 2 бага — обрыв SSE на долгих паузах и путаницу агента с абсолютными `/workspace/...` путями — оба исправлены и выкачены (2026-07-28). **Второй живой прогон в тот же день вскрыл ещё 4 бага живого чата** (пустые пузыри, одинаковое время у всех сообщений, чат не обновляется сам на многошаговых ходах, итоговый файл не виден в дереве) — диагностика завершена, спек написан
(`docs/superpowers/specs/2026-07-28-web-chat-live-view-fixes-design.md`). **Все 4 фикса реализованы, прошли ревью, влиты в `main`, выкачены на VPS и подтверждены живой приёмкой (2026-07-28)** — детали в «Сейчас в работе» ниже. **Живая приёмка (Task 5) по пути вскрыла и потребовала починить ещё 2 отдельные проблемы, не связанные с 4 фиксами**: (a) баг в самом фиксе Task 3 — живой вид на многошаговом ходе расходился с перезагрузкой (первопричина: SSE-контракт Hermes шлёт `message.started`/`assistant.completed` один раз на весь ход, а не на каждый шаг, вопреки предположению спека) — исправлено надёжной пере-сверкой с историей после `event: done`; (b) **критичная инфраструктурная находка**: `write_file` при `terminal.backend: docker` реально исполняется внутри docker-песочницы и терял файлы (`HERMES_WRITE_SAFE_ROOT` не совпадал с реально смонтированным `/workspace`, плюс контейнер писал файлы от `root`, недоступные `hermes-web`) — оба слоя исправлены на сервере (`.env` + пересоздание контейнера с `docker_run_as_host_user`) и в коде (`quickchat.py` системное сообщение), подтверждено вживую. **Тот же класс бага всплыл снова на следующий день (2026-07-29)** — живой прогон разбора Кирика 3.23–3.29 упёрся в root-owned файлы в другой части workspace; на этот раз реализована и задеплоена автоматическая самопочинка (D-012, узкий sudo для `hermes` на один root-скрипт, вызывается перед каждым ходом чата и файловой операцией UI). **Тот же живой прогон отдельно вскрыл ещё 2 проблемы дифференциации контекста**: пустая папка, созданная в `result/` через UI, не отображалась в дереве (сама папка была невидима для API, а не только для интерфейса), и модель почти всё складывала мимо `result/`, несмотря на системное сообщение. Обе исправлены (D-013): `list_tree` теперь видит пустые директории, новый селектор «папка для результата» в композере чата усиливает системное сообщение конкретным путём на каждый ход — реализовано через subagent-driven-development, задеплоено, живая проверка на сервере пройдена. **В тот же день, сразу после деплоя D-013, следующий живой ход в том же проекте показал: баг с размещением файлов не исчез** — модель снова не спросила и снова не положила в `result/`. Расследование через прямой запрос к Hermes API (не теория) показало причину: это не игнорирование инструкции, а устоявшийся за много ходов прецедент в истории того же длинного диалога (часть файлов в этом проекте датирована на 5 дней раньше, чем вообще появился `source/outer/result`) — одного абзаца системного сообщения на один ход недостаточно, чтобы перевесить. Написан черновик спека с планом действий (диагностическое логирование + ручная кнопка «→ result/» как надёжный фолбэк, не полагающийся на модель) — пользователь вернулся и согласовал решения (2026-07-29): **делаем A1 (логирование) + A2, обобщённое до общего действия «переместить/переименовать» на любом файле/папке в любом бакете** (не только вытащить из `misc`, но и свободно перестроить структуру проекта), **A3 не делаем**; для сворачиваемых папок — **дефолт «всё свёрнуто» при каждом открытии проекта** (не «развёрнуто», как было в черновике). По пути нашёлся и добавлен в тот же спек смежный баг: файлы в `misc` открывались только на скачивание, хотя `.md`/`.txt` в остальных buckets открываются в редакторе — причина глубже фронтенда (`workspace.save_file` явно отклоняет пути вне `source/outer/result`), фикс включает и backend. Пользователь отдельно обозначил будущее направление — редактирование скилов/команд в рамках проекта — объём пока не уточнён, зафиксировано как идея на будущее, не как часть текущего плана. **План реализации написан и выполнен целиком (D-014, 2026-07-29)** через subagent-driven-development — 6 задач + 1 фикс-раунд (критичная находка в `move_entry`) + финальное ревью всей ветки (2 критичные + 2 важные, все закрыты одним фикс-проходом; один Important-финдинг самой фикс-волны сознательно запаркован). 175 → 201 тест, слито в `main`, запушено в GitHub, **задеплоено на VPS и проверено вживую** (одноразовый `qa_temp`, все находки финального ревью — включая ровно ту, что билась с реальным misc-сценарием пользователя, — подтверждены исправленными на живом сервере, `qa_temp` удалён). Один второстепенный минус (глушится access-лог в journald) сознательно оставлен на потом. **MVP-часть D-014 закрыта целиком.** Matrix отключён от приоритета (D-008). **Бэклог живого тестирования рабочего экрана (2026-07-31…2026-08-01): Группа B (папки бакетов), Группа A (CRUD-гигиена — удаление/создание/теги) и Группа C (редирект «Быстрого чата» на полноценный `project-workspace.html`, удаление мёртвого `chat.html`) закрыты целиком, все три задеплоены на VPS и подтверждены живой приёмкой через реальный HTTP API — детали ниже в «Сейчас в работе».** **Группа D (layout-редизайн: модальный редактор → раздвижная панель) реализована через subagent-driven-development (3 задачи, все ревью чистые), задеплоена на VPS и подтверждена живой приёмкой пользователя (2026-08-02) — весь бэклог живого тестирования (B→A→C→D) закрыт целиком.** **Под-проект C (админ-панель) реализован целиком (2026-08-02)**: спека согласована с пользователем (карточка кредитов wormsoft.ru убрана из среза — у провайдера нет публичного API для этих цифр, проверено эмпирически; логин пользователя неизменяем; журнал логирует только крупные события), план из 5 задач выполнен через subagent-driven-development (все 5 задач — ревью чистые), финальное ревью всей ветки нашло 2 важные + 3 второстепенные находки — все закрыты одним фикс-проходом (silent-fail на обзоре метрик при ошибке /proc, вводящая в заблуждение метка «онлайн» вместо «за 30 дней», мёртвая навигация в сайдбаре, журнал не обновлялся после действий админа, макетные подписи карточек), scoped re-review подтвердил все устранёнными без новой поломки. Одна находка — путь в `detail` событий журнала логируется абсолютным (не `<группа>/<slug>`, как в спеке) — расхождение спеки и плана, пользователь решил оставить как в плане. 249 → 288 тестов (`hermes-web`). Слито в `main` локально (fast-forward), worktree и ветка-фича удалены. **Задеплоено на VPS и подтверждено живьём (2026-08-02):** резервные копии в `~/.hermes-web-backups/<TS>/` перед `rsync` семи файлов (`hermes_web/{app,storage,projects,quickchat,admin_metrics}.py`, `static/{admin,home}.html`) + чистый `systemctl --user restart hermes-web.service`, без ошибок в journalctl. **Живая приёмка через реальный HTTP API** (одноразовые `qa_temp`/`qa_temp2`/`qa_temp3`, все удалены после проверки вместе с двумя осиротевшими строками `qa_temp` в `project_index/index.db`, найденными по пути — не регрессия этого среза, наследие прежних ручных QA-сессий): `/api/admin/overview` вернул реальные метрики сервера (CPU, RAM, диск `/home/hermes/workspace`, счётчик активных сессий); `/api/admin/users` вернул реальных `dem`/`rost` с корректными `project_count`/`last_active`; создание пользователя, дубликат логина (409), невалидный логин (400), самопонижение роли (400), правка роли/имени, сброс пароля — всё подтверждено против настоящего сервера; owner-гейт подтверждён по всем трём кодам (401 без сессии, 403 участником, 200 владельцем); журнал событий отразил каждое действие. **Под-проект C закрыт целиком, в проде.** Следующая точка ветвления пользователем не выбрана.

---

## Где мы сейчас

VPS заказан и доступен (D-002), аккаунты настроены (D-003: `hermes` без
sudo, свой ключ). Hermes CLI 0.19.0 установлен на сервере под `hermes`
и обвязан архитектурой «клетки» (D-004): Docker-сэндбокс для shell-команд
с лимитами ресурсов, approvals в режиме manual + свои deny-правила,
tirith-сканер, write-safe-root, Python-плагин content-injection-guard
против prompt injection из веб-контента/картинок, правило в SOUL.md.

Matrix-шлюз настроен и проверен (D-005): подключение к домашнему серверу
заказчика (`matrix.ctvuprising.ru`) под `@hermes`, access token в `.env`,
allowlist на 2 человек (заказчик + сын). Тестовый запуск дал `✓ matrix
connected`.

Провайдер моделей wormsoft.ru подключён и роутинг по ролям прописан
(D-001): conductor = `wormsoft/agent/high`, vision/web_extract/compression
— дешёвые тиры через `auxiliary.*`, worker (сабагенты) — `wormsoft/agent/low`.
Ключ лежит в `.env` как `WORMSOFT_API_KEY` (важно: НЕ `OPENAI_API_KEY` —
Hermes резолвит ключ по домену эндпоинта, наступили на эти грабли один
раз, задокументировано в D-001). Проверено `hermes -z` на двух тирах —
отвечает.

Веб-поиск подключён (D-006): инструмент `web_search` работает на `ddgs`
(бесплатно, без ключа) — проверено реальным запросом («погода в Москве»),
агент ответил актуальными цифрами. Perplexity **не подключить как backend
поиска** — выяснилось, что это отдельная chat-модель, а не поисковый
backend; ждём от заказчика, есть ли у него именно API-ключ Perplexity
(не путать с потребительской подпиской Pro), чтобы подключить её отдельной
моделью для роли «Блок поиска».

Шлюз поднят как постоянный пользовательский systemd-сервис
(`hermes gateway install --start-now --start-on-login`, linger включён —
переживёт разлогин SSH и перезагрузку). `hermes gateway status` —
`active (running)`.

**Первый сквозной диалог подтверждён заказчиком** — в комнате «Hermes
room» (Element) бот ответил на «Привет, ты тут?». По пути пробовали
включить E2EE — не завелось после трёх попыток (протокольный баг на
стороне Element/mautrix, не наш конфиг), отключили обратно (D-007).
Старая зашифрованная комната-«тест» — мертва навсегда (Matrix не даёт
снять шифрование с комнаты задним числом), использовать только «Hermes
room» или новые незашифрованные комнаты.

**Агент полностью живой и отвечает в Matrix.** Проверили и роутинг
моделей: `wormsoft/agent/high` в моменте резолвится в `minimax-m3`, но
может уйти на `kimi-k2.6`/`zai/glm-5.1` — Hermes не логирует фактическую
модель, только запрошенный алиас.

Веб-дашборд Hermes поднят постоянно (`hermes-dashboard.service`, systemd
user-юнит под `hermes`, `enable --now`, linger уже включён — переживёт
перезагрузку VPS). Биндится строго на `127.0.0.1:9119` на сервере (без
аутентификации, т.к. наружу не торчит) — доступ снаружи только через
SSH-туннель. Заказчик открыл его у себя в браузере через
`ssh -L 9119:127.0.0.1:9119 root@212.115.55.116` в PowerShell — подтверждено
скриншотом (Chat/Skills/Models/Config и т.д., 65 скиллов в 12 категориях).

Браузер-инструменты (`browser_navigate`/`browser_snapshot`, Playwright/
Chromium) починены — раньше `npm install` падал под root из-за отсутствия
node в PATH, доустановили под `hermes`. Проверено живым вызовом (агент
реально открыл example.com и прочитал title). Вместе с уже работавшим
`ddgs`-поиском (D-006) это закрывает запрос заказчика «искать в интернете
и заходить на любую страницу» — **без Perplexity**, бесплатно, без новых
API-ключей.

**Живой прогон дошёл до результата**: скан задачника распознан через
`vision_analyze` (после переключения `auxiliary.vision.model` на
`wormsoft/vision/high` — `vision/low` в этот день стабильно таймаутил),
заказчик получил разбор и попросил решить все 12 задач с генерацией
файлов. `delegation.model` (суб-агенты) поднят с `low` до `medium` —
компромисс по итогам сегодняшних зависаний (в основном из-за
перегруженного wormsoft.ru, не из-за тира модели, см. D-001).

**Важно:** обнаружили и починили, что `SOUL.md` (правила поведения
агента, D-004) блокировался встроенным антиинъекционным сканером Hermes
с самой первой настройки сервера — наш же пример фразы-инъекции внутри
правила триггерил фильтр. Все правила безопасности из SOUL.md реально
заработали только сейчас, задним числом. Заодно объединили рабочую
папку агента (`/workspace` внутри песочницы = write-safe-root на хосте
= дашборд FILES = доставка вложений) — раньше это были три разные,
несвязанные папки.

Параллельные задачи в Matrix настроены и проверены живьём: нашли и
починили `MATRIX_DM_AUTO_THREAD=false` (default, из-за которого «Hermes
room» — Hermes сам считает её DM — не открывала ветки), теперь `.env`
содержит `MATRIX_DM_AUTO_THREAD=true`. Заказчику собрана шпаргалка
[docs/manual.md](./manual.md) — как ставить задачи параллельно (`/bg`),
не путаясь в одной ленте, таблица слэш-команд, что означает «waiting on
provider» (это wormsoft.ru временно тормозит, не наш баг — Hermes сам
переподключается). Пригодится при тиражировании проекта друзьям
заказчика.

## Сейчас в работе

**Группа A бэклога живого тестирования (удаление проекта → создание
проекта из списка → редактирование тегов/статуса) — реализована через
subagent-driven-development, слита в `main`, задеплоена на VPS и
подтверждена живой приёмкой (2026-08-01). Готово целиком.**

- Спек: [superpowers/specs/2026-07-31-project-crud-hygiene-design.md](./superpowers/specs/2026-07-31-project-crud-hygiene-design.md).
  План: [superpowers/plans/2026-07-31-project-crud-hygiene.md](./superpowers/plans/2026-07-31-project-crud-hygiene.md)
  (6 задач: A1/A2/A3 бэкенд/фронтенд, TDD, свежий субагент-исполнитель на
  задачу + ревью после каждой + финальное ревью всей ветки на самой
  мощной модели, worktree `project-crud-hygiene`).
- **Пре-флайт находка ДО начала реализации**: сверка плана с оригинальным
  спеком перед диспетчеризацией задач вскрыла, что черновик A2
  (`create_project`) строил путь нового проекта голым `os.path.join` без
  валидации — `group` приходит из HTTP-тела непроверенным, значит
  `group="../rost"` вывел бы проект в чужое пространство пользователя.
  Тот же класс уязвимости, что ревью 2026-07-26 нашло и закрыло в
  `move_project`. Исправлено в самом плане до дисптача — `create_project`
  валидирует итоговый путь через уже существующий `resolve_project_path`,
  плюс traversal-тест на обоих уровнях (функция + HTTP-роут), закрыто до
  единой строки продакшен-кода.
- **Task 1 (A1 бэкенд)** — 1 критичная находка ревью, исправлена одним
  раундом: обёртка `delete_project` в `hermes_web/projects.py` передавала
  `api_key` в плагинную функцию, которая такой параметр не принимает —
  падение `TypeError` на любом реальном деплое с заданным
  `WORMSOFT_API_KEY`. Тесты этого не ловили, т.к. фикстура жёстко ставила
  `wormsoft_api_key=None`. Исправлено переключением на уже существующий
  `_workspace_kwargs`, добавлен регрессионный тест.
- **Task 6 (A3 фронтенд)** — 1 важная находка ревью, исправлена одним
  раундом: редактирование тегов оптимистично обновляло UI до ответа
  сервера, без отката при неудаче — теги в интерфейсе могли разойтись с
  реально сохранёнными на диске. Исправлено снапшотом `prevTags` +
  откатом + повторным рендером при ошибке.
- **Финальное ревью всей ветки** (самая мощная модель, реально
  эксплуатировала находки, не просто читала код) нашло 3 важные + 1
  второстепенную (повышена до «исправить сейчас»), закрыто одним
  фикс-проходом + одним scoped re-review: (1) `reindex_all` воскрешал
  удалённые в `.trash/` проекты — `os.walk` не пропускал скрытые папки;
  (2) `create_project` принимал враждебные `group` (`.trash`, вложенные
  пути, пустую строку) — не эскейп из пространства пользователя (эта
  уязвимость уже закрыта пре-флайтом), а API-гигиена; (3) `tags`/`status`
  из HTTP-тела не проверялись на тип — нетипизированный `tags` реально
  портил данные на диске (строка "физика" превращалась в шесть
  однобуквенных тегов при чтении), невалидный `status` делал проект
  невидимым в обоих фильтрах UI без пути восстановления кроме SSH; (4)
  `yaml.safe_dump` без `sort_keys=False` пересортировывал весь
  YAML-блок фронтматтера при правке одного поля. Один второстепенный
  риск (гонка при двух быстрых подряд операциях с тегами, если более
  ранний запрос падает после того, как более поздний уже успешно
  применился) сознательно запаркован — задокументированный, не блокирует.
- 74 → 76 тестов (`hermes-plugins`), 219 → 247 тестов (`hermes-web`).
  Фронтенд — без автотестов (нет JS-фреймворка в репозитории), но каждая
  фронтенд-задача проверена построчной трассировкой скобок/шаблонных
  строк ревьюером (Task 6 — с двойной проверкой через
  `node -e "new Function(...)"`, независимо перезапущенной ревьюером).
- **Слито в `main` локально (fast-forward), запушено в GitHub.** Worktree
  и ветка-фича удалены (`ExitWorktree`, `discard_changes: true` —
  безопасно, все 11 коммитов уже были достижимы из `main` после
  fast-forward).
- **Задеплоено на VPS** (2026-08-01): резервные копии в
  `~/.hermes-web-backups/<TS>/` и `~/.hermes/backups/<TS>/` перед
  `rsync`; `rsync` пяти изменённых файлов
  (`hermes_web/{app,projects,quickchat}.py`,
  `static/project-selector.html`, `project_index/core.py`) + чистый
  `systemctl --user restart` **обоих** сервисов
  (`hermes-web.service` и `hermes-gateway.service`, как и требовал
  общий плагинный файл) — без новых трейсбеков в journalctl (уже
  известный, не связанный с этим деплоем артефакт: `hermes-gateway`
  всегда логирует `exit-code status=1` при остановке на каждом рестарте,
  подтверждено сверкой истории journalctl за последние 7 рестартов —
  не регрессия).
- **Живая приёмка через реальный HTTP API** (одноразовый `qa_temp`,
  создан/удалён inline через `storage.create_user`/прямые SQL-запросы,
  без браузера — через `curl` с cookie jar по SSH): A2 создал проект с
  полным скаффолдом на диске в указанной группе; A2 отклонил враждебные
  `group` (`.trash`, `1C/deep`) с 400 и ничего не создал; A3 записал теги
  в YAML-фронтматтер, не тронув остальные секции `about.md`, и отклонил
  невалидные `tags`/`status` с 400; A1 переместил проект в `.trash/` со
  штампом, `.trash` не отображается в `/api/groups`; `reindex_all`,
  запущенный вручную против `qa_temp`, подтвердил — удалённый проект
  **не** воскрес. Побочно найдены и подчищены 6 "осиротевших" строк в
  индексе от прошлых ручных QA-сессий (до этой фичи `delete_project`
  не существовал, ручной `rm -rf` не чистил SQLite-индекс) — не
  регрессия этого среза, гигиена наведена по пути.

**Минорные находки ревью, сознательно оставлены на потом (не блокируют):**
инconsistent сообщения об ошибках между кнопками удаления/создания/тегов
в `project-selector.html` (только кнопка создания показывает текст
ошибки сервера); `saveTags()` не обновляет `renderMain()` (тег-облако
устаревает до переоткрытия панели), а переключатель статуса не вызывает
`loadProjects()` (архивированный проект остаётся в списке `active` до
переоткрытия); неиспользуемый параметр `db_conn` в `create_project`
(сознательная симметрия с `create_quick_chat`); мёртвый CSS `.p-tags`/
`.p-tags span` в `project-selector.html`; `update_project_metadata` не
имеет отдельного cross-user/traversal-теста (защита разделяется с уже
протестированными `delete_project`/`create_project`).

**Живой баг сразу после деплоя Группы A (2026-08-01), найден и исправлен
в тот же день.** Реальный пользователь `dem` не смог удалить проект
через новую кнопку — «Не удалось удалить проект». `systematic-debugging`:
логи сервера чисты (запрос дошёл, `delete_project` штатно вернул 400),
причина — в данных: у `dem` в группе `ALL` две «призрачные» строки в
индексе без папки на диске (папки пропали ещё до появления
`delete_project`, тот же класс проблемы, что уже чистили у `qa_temp`).
`delete_project` был прав, что кидал ошибку, но у пользователя не было
способа убрать такую сломанную карточку никак. Исправлено: (1) обе
призрачные строки `dem` вычищены из индекса на проде; (2)
`project_index_core.delete_project` доработана — если `about.md` не
найден, но строка в индексе есть, считать это «призраком» и просто убрать
строку (не кидать ошибку); если строки тоже нет — поведение не менялось.
TDD на обоих уровнях (`core.py` + обёртка `hermes_web/projects.py`,
последняя — специально под `chat_sessions.project_path NOT NULL`), тесты
76→77 / 247→248, задеплоено на VPS (оба сервиса перезапущены),
закоммичено точечно в `main` без отдельного worktree — маленький
изолированный хотфикс. Подробности — `changelog.md`.

**Группа C бэклога живого тестирования (2026-08-01) — реализована,
слита в `main`, задеплоена на VPS и подтверждена живой приёмкой. Готово
целиком.** «Быстрый чат» на `home.html` вёл на устаревший минимальный
`chat.html` (голый тред, без дерева файлов) вместо полноценного
`project-workspace.html`. Реализовано через `subagent-driven-development`
в изолированном worktree по плану из 1 задачи (`docs/superpowers/plans/2026-08-01-quickchat-workspace-redirect.md`)
— чисто фронтенд-правка: редирект переключён на
`project-workspace.html?path=<project_path>`, `chat.html` удалён как
мёртвый код целиком, два комментария (`app.js`, докстринг `hermes_web/app.py`),
что на него ссылались, обновлены. Бэкенд не тронут — `project-workspace.html`
уже сам вызывает `POST /api/projects/open`, которая через
`get_or_open_session` находит уже существующую чат-сессию для того же
`project_path` (созданную `create_quick_chat` на предыдущем шаге) и
переиспользует её, не плодя вторую. Задача-ревью — чисто (Approved, 0
находок). Финальное ревью всей ветки на самой мощной модели — тоже чисто
(«Ready to merge: Yes»), архитектурное утверждение плана (переиспользование
сессии, отсутствие race) отдельно эмпирически перепроверено ревьюером
(не только чтением кода), 5 минорных находок: 2 исправлены одним
фикс-проходом (регресс-тест, запирающий инвариант переиспользования
сессии + синхронизация списка страниц в докстринге `app.py` со списком в
`app.js`), scoped re-review подтвердил обе устранёнными без нового
поломанного; 3 запаркованы с решением контроллера — удаление `chat.html`
на сервере (деплой — rsync, не git pull, поэтому git-delete сам по себе
файл на проде не уберёт), обновление `state.md`/`changelog.md` (этот
проход), отсутствие `UNIQUE(user, project_path)` у `chat_sessions`
(предсуществующее, не связано с этой веткой, недостижимо через этот
флоу). Тесты 77 / 248→249. Слито в `main`, запушено в GitHub, worktree
убран. Задеплоено на VPS (3 файла rsync + явное удаление `chat.html` на
сервере + рестарт `hermes-web.service` — `hermes-plugins` не менялся,
`hermes-gateway.service` перезапускать не нужно). **Живая приёмка через
реальный HTTP API** (одноразовый `qa_temp2`, не только UI): `POST
/api/quick-chat` → `POST /api/projects/open` с тем же `project_path`
вернули **идентичный** `chat_session_id`, в `chat_sessions` на сервере
подтверждена ровно одна строка (не задвоилась), `GET /chat.html` → 404,
`GET /project-workspace.html` → 200 — `qa_temp2` полностью вычищен после
проверки (БД, workspace, index.db).

**Группа D — реализована, задеплоена на VPS, подтверждена живой приёмкой
и прошла финальное ревью с фикс-проходом (2026-08-02). Готово целиком,
бэклог живого тестирования (B→A→C→D) закрыт.** Модальный редактор файлов
заменён раздвижной панелью между деревом и чатом (Flexbox вместо CSS
Grid); дерево/редактор/боковая панель — ручной resize + сворачивание,
персистентно в `localStorage`; несохранённые правки защищены `confirm()`.
План: [superpowers/plans/2026-08-01-workspace-layout-redesign.md](./superpowers/plans/2026-08-01-workspace-layout-redesign.md)
(3 задачи, `subagent-driven-development`). Живая приёмка нашла и сразу
закрыла расширение ширины редактора до `max(560px, 50% рабочей области)`.
**Финальное ревью всей ветки** (после приёмки) нашло 3 Important-находки
на стыке задач — все закрыты одним фикс-проходом, задеплоено: (1) ширина
редактора теперь клампится при восстановлении и потолок пересчитывается
на старте каждого drag, а не один раз при загрузке; (2) перемещение (`→`)
открытого сейчас в редакторе файла переуказывает редактор на новый путь
(раньше следующее «Сохранить» тихо воскрешало файл по старому пути);
(3) повторный клик по уже открытому файлу с чистым (нередактированным)
буфером теперь перечитывает содержимое с диска, а не остаётся
безусловным no-op — это, вероятно, и есть причина более ранней жалобы
«не сохраняет» (см. следующий абзац). Полная хроника, включая инцидент с
перезаписью `AGENTS.md` пользователя при диагностике и восстановление
байт-в-байт из истории сессии Hermes — в `changelog.md`.

**Тем же днём, по дополнительной живой обратной связи (2026-08-02) —
два точечных фикса сверх Группы D, задеплоены:**
1. Шаблон `AGENTS_MD_TEMPLATE` (`quickchat.py`, применяется к новым
   проектам) получил раздел «Три файла в корне», явно объясняющий роль
   `about.md` (раньше о нём не было ни слова) и разграничивающий
   `AGENTS.md`/`history.md` — живой прогон показал, что агент путает эти
   два файла между собой без явного указания.
2. Поле ввода чата (`#composeInput`) теперь растёт по высоте при вводе
   (40–200px) и получило drag-хендл для ручного растягивания до 480px,
   персистентно в `localStorage` — тот же паттерн, что панели Группы D.

Под-проект C (админ-панель) реализован, слит в `main` и задеплоен на VPS
(2026-08-02, детали выше) — закрыт целиком.

---

**D-015 (пакет стартовых файлов проекта about.md+AGENTS.md+history.md +
подхват AGENTS.md в системное сообщение) — реализовано, задеплоено на
VPS и подтверждено живьём (2026-07-31). Готово целиком.** Закрывает
находку из живого разговора пользователя с агентом через `hermes-web`
(`AGENTS.md` лежал на диске, но не влиял на модель). Спека:
[superpowers/specs/2026-07-30-project-bootstrap-files-design.md](./superpowers/specs/2026-07-30-project-bootstrap-files-design.md),
план: [superpowers/plans/2026-07-30-project-bootstrap-files.md](./superpowers/plans/2026-07-30-project-bootstrap-files.md)
(5 задач, TDD, Subagent-Driven Development). Реализовано: каждый новый
проект сразу получает `about.md`+`AGENTS.md`+`history.md`; старые проекты
дополучают недостающие файлы лениво при следующем открытии
(`_backfill_root_files`, с самопочинкой — ошибка записи логируется, а не
роняет открытие проекта); `_agents_md_block` читает `AGENTS.md` (лимит
4000 символов, обрезка с пометкой, пустой/нечитаемый файл — не ошибка);
`_system_message_for` подмешивает его в конец system-сообщения.
**Критичная находка финального ревью, исправленная в той же ветке:**
универсальный текст конвенций (`result/`/`source/`/`outer/`, «проверяй
ответ инструмента») изначально остался задублирован и захардкожен в
Python **и** в шаблоне `AGENTS_MD_TEMPLATE` одновременно — то есть
правка `AGENTS.md` пользователем не могла реально переопределить
поведение модели, конвенции просто печатались дважды. Исправлено:
захардкоженный текст в `_system_message_for` теперь используется только
как фолбэк на случай, если `agents_md_block` пуст (проект без
`AGENTS.md` — сегодня это только редкий обходной путь через прямой
`chat.html?id=...`, не основной поток), а не всегда. 175 → 215 тестов.
**Задеплоено на VPS (2026-07-31):** единственный изменённый файл —
`hermes_web/quickchat.py` (rsync + бэкап предыдущей версии в
`~/.hermes-web-backups/` на сервере) + чистый `systemctl --user restart
hermes-web.service` (без ошибок в журнале). **Живая приёмка пройдена**
одноразовым `qa_temp` (создан inline через `storage.create_user`+
`auth.hash_password`, удалён вместе с проектом и chat_session после
проверки): создание «Быстрого чата» сразу породило `about.md`+
`AGENTS.md`+`history.md` в корне нового проекта; правка `AGENTS.md`
через `POST /api/projects/file` (тот же путь, что и UI-редактор)
добавлением одноразового маркерного правила была реально подхвачена —
следующий ход чата (`POST /api/chat/{id}/send`) начал ответ ровно с
этого маркера, подтверждая, что `AGENTS.md` реально влияет на поведение
модели, а не только существует на диске. Лог `hermes_web.quickchat`
пишется в журнал (`INFO:hermes_web.quickchat:chat_session_id=... 
result_target=None reinforced=False` — `reinforced` относится к
`result_target`, не к `AGENTS.md`, ожидаемо `False` без выбранной папки
результата). Под-проект C (админ-панель) или Часть D (скилы/команды в
рамках проектов) остаются на будущее по уточнению пользователя.

---

**Бэклог живого тестирования рабочего экрана (2026-07-31) — новый
приоритетный следующий шаг, реализация не начата.** Сразу после деплоя
D-015 пользователь прогнал `hermes.blackboxbegin.space` живьём и прислал
8 находок (баги + фич-запросы) с двумя скриншотами. Спек:
[superpowers/specs/2026-07-31-workspace-ux-backlog-from-live-testing.md](./superpowers/specs/2026-07-31-workspace-ux-backlog-from-live-testing.md) —
каждая находка привязана к конкретному месту в коде (не гипотезы),
сгруппирована по риску/размеру дизайн-решений:

- **Группа B** (самодостаточная, без открытых вопросов): создание проекта
  сегодня заводит только `about.md`/`AGENTS.md`/`history.md`
  (`create_quick_chat`), но не папки `source/outer/result` — нужно
  расширить `_backfill_root_files`. **B2 (видимость корневых файлов в
  дереве) подтверждена пользователем как уже закрытая текущим UI — работы
  не требует.** **B1 (папки) — реализовано, задеплоено на VPS и
  подтверждено вживую (2026-07-31). Готово целиком.** Спек:
  [2026-07-31-project-scaffold-folders-design.md](./superpowers/specs/2026-07-31-project-scaffold-folders-design.md),
  план: [2026-07-31-project-scaffold-folders.md](./superpowers/plans/2026-07-31-project-scaffold-folders.md)
  (2 задачи, TDD, Subagent-Driven Development в изолированном worktree —
  каждая задача с отдельным ревью + финальное ревью всей ветки на самой
  мощной модели). Реализовано: `_backfill_root_files` переименован в
  `_backfill_project_scaffold`, кроме трёх файлов теперь создаёт папки
  `source/outer/result` через `os.makedirs(..., exist_ok=True)` по списку
  `workspace.BUCKETS` (единственный источник правды, без второго
  хардкода — тот же класс дублирования уже чинили в D-015). Ошибка
  создания одного бакета логируется и не прерывает остальные (симметрично
  файловой части). **Финальное ревью (самая мощная модель) нашло 1
  Important-находку — не про код, а про план**: шаг живой приёмки в плане
  проверял результат через дерево `project-workspace.html`, но
  `list_tree` рисует заголовки всех трёх бакетов безусловно, независимо
  от того, существуют ли папки реально — то есть такая проверка
  ложно-зелёная и не отличит успешный деплой от сорванного. Исправлено
  прямо в плане (без код-ревью — не код): живая приёмка должна сверяться
  по диску сервера (`ls -la`), не по UI. 2 второстепенные находки
  запаркованы (устаревшее имя функции в этом файле — уже поправлено этой
  же правкой; некритичный `caplog`-ассерт в тесте отказоустойчивости).
  219/219 тестов зелёные на слитом результате. **Слито в `main` локально
  (fast-forward), запушено в GitHub, worktree и ветка-фича удалены.**
  **Задеплоено на VPS** (2026-07-31): `rsync hermes_web/quickchat.py`
  (единственный изменённый файл) + чистый `systemctl --user restart
  hermes-web.service`. **Живая приёмка — по диску сервера, не по UI**
  (как поправлено ревью): новый проект `qa_temp` сразу получил
  `source/outer/result` рядом с тремя файлами (подтверждено `ls -la` на
  сервере); реальный старый проект `dem/ai-agenty-i-modeli/chat-c9865316`
  (создан до этой фичи, имел только `result/`) — после вызова
  `get_or_open_session` (существующая Hermes-сессия переиспользована, без
  побочных эффектов на реальный диалог) на диске появились `source/` и
  `outer/`, существующие файлы не тронуты. `qa_temp` удалён.
- **Группа A** (CRUD-гигиена, независимые бэкенд+UI задачи): удаления
  проекта нет вообще ни в коде, ни в UI (2 тестовых «мусорных» проекта
  реально нечем убрать); создания проекта из `project-selector.html` с
  привязкой к группе под курсором тоже нет (сегодня единственный путь —
  «Быстрый чат», всегда в `ALL`); редактирования тегов нет — `tags`/
  `status` это YAML-фронтматтер `about.md`, правится только как сырой
  текст.
- **Группа C** (архитектурное упрощение): «Быстрый чат» на `home.html`
  ведёт на старый минимальный `chat.html` (без дерева файлов и
  «Активности агента») вместо полноценного `project-workspace.html` —
  отсюда «пустой экран» на скриншоте пользователя. Предложение —
  переключить редирект и удалить `chat.html` как мёртвый код.
- **Группа D** (крупнейшая, нужен отдельный брейнсторминг перед
  спеком): модальный редактор файлов → раздвижная VS-Code-подобная
  панель между деревом и чатом; узкая колонка чата → расширяемая
  область. Оба пункта, вероятно, закрываются одним общим layout-
  редизайном, не двумя отдельными задачами.

**Рекомендованный порядок подтверждён пользователем: B → A (удаление →
создание → теги) → C → D.** **Группа B закрыта целиком (B1 задеплоен и
подтверждён вживую, B2 не требовала работы).** Следующий шаг — Группа A
(удаление проекта → создание проекта из `project-selector.html` → теги),
`writing-plans` можно начинать сразу без брейнсторма (открытых
дизайн-вопросов нет). Для C — тоже без брейнсторма. Для D — сначала
`brainstorming` по интеракции layout.

---

**D-014 реализован, слит в `main`, задеплоен на VPS и проверен вживую
(2026-07-29). Готово целиком — MVP-часть закрыта, следующий шаг после
этого — под-проект C (админ-панель) либо Часть D (скилы/команды в
рамках проектов), когда пользователь сузит объём.**

- Спек: `docs/superpowers/specs/2026-07-29-result-placement-reliability-and-collapsible-tree-design.md`.
  План: `docs/superpowers/plans/2026-07-29-result-placement-reliability-and-collapsible-tree.md`
  (6 задач, TDD, Subagent-Driven Development — свежий субагент-исполнитель
  на задачу + ревью после каждой + финальное ревью всей ветки на самой
  мощной модели).
- Реализовано (детали решений — D-014 в `decisions.md`):
  - **A1** — `quickchat.send_message` логирует `chat_session_id`/
    `result_target`/флаг «усиление добавлено» на каждый ход через
    стандартный `logging` (не print/stdout, не полный текст системного
    сообщения).
  - **A2** — общее действие «переместить/переименовать»: новая функция
    `move_entry` (`hermes_web/workspace.py`) + эндпоинт
    `POST /api/projects/move-entry` (`hermes_web/app.py`) + кнопка «→» на
    каждом узле дерева (`static/project-workspace.html`) — работает на
    любом файле/папке, в любом bucket'е (`source`/`outer`/`result`/
    `misc`), не только «в result/».
  - **Часть B** — сворачиваемые вложенные папки, дефолт при каждом
    открытии проекта — всё свёрнуто; `localStorage` по проекту хранит
    только отклонения (что пользователь явно развернул).
  - **Часть C** — `save_file` больше не отклоняет пути вне
    `source/outer/result` (защита от path traversal через
    `resolve_file_path` осталась); фронтенд убрал специальный случай для
    `misc` — `.md`/`.txt` там тоже открываются в редакторе.
  - **Часть D (на будущее, не в этом срезе):** пользователь упомянул
    желание создавать/редактировать скилы и команды в рамках проектов —
    объём не уточнён, нужен отдельный брейнсторминг позже.
- **Fix-раунд Task 3** (критичная находка): защита `ROOT_EDITABLE_FILES` в
  `move_entry` сравнивала сырую строку вместо резолвленного пути —
  `./about.md` обходил проверку и позволял вынести `about.md` из проекта.
  Исправлено сравнением резолвленных путей.
- **Финальное ревью всей ветки** нашло 2 критичные + 2 важные + 1
  второстепенную находку, закрыты одним фикс-проходом: (1) логирование A1
  никогда не доходило до продакшена — нигде не вызывался
  `logging.basicConfig`; (2) кнопка «переместить» не работала для папок в
  разделе «остальное» — синтетический ключ отображения `misc/...`
  использовался как реальный путь источника; (3) сохранение `about.md`
  через `./about.md` больше не запускало переиндексацию — та же
  уязвимость, что и в находке Task 3, отдельно найденная в `save_file`;
  (4) `move_entry` падал с 500 вместо чистой ошибки на `dest_dir`,
  указывающем на файл вместо папки. Один Important-финдинг из самой
  фикс-волны (отключение проброса `aiohttp.access`-лога глушит access-лог
  в journald) сознательно запаркован как отложенный, не блокирующий.
- 175 → 201 тест (backend). Фронтенд-часть — без автотестов (нет JS-
  фреймворка в репозитории), но живая проверка прошла через реальный
  API + сверку задеплоенного `project-workspace.html` (см. ниже).
- **Слито в `main` локально (fast-forward), запушено в GitHub.**
  Feature-ветка и worktree удалены (`ExitWorktree`, `discard_changes:
  true` — безопасно, всё уже в `main`).
- **Задеплоено на VPS** (2026-07-29): `rsync` пяти изменённых файлов
  (`hermes_web/{app,quickchat,workspace}.py`, `run.py`,
  `static/project-workspace.html`) + чистый `systemctl --user restart
  hermes-web.service` (без ошибок в журнале, `login.html` → 200,
  `/api/me` без авторизации → 401).
- **Живая приёмка** — одноразовый аккаунт `qa_temp` + проект
  `qa_temp/ALL/smoke-test`, оба удалены после проверки:
  - A1: `configure_logging()` реально поднимает эффективный уровень
    root-логгера до `INFO` — прямой вызов `logger.info(...)` из
    `hermes_web.quickchat` теперь пишется, а не тихо отбрасывается
    (было воспроизведено на живом процессе до фикса D-014, и на
    сервере после деплоя явно подтверждено, что вызов проходит).
  - A2: `POST /api/projects/move-entry` реально переносит и файл, и
    **папку** — включая ровно тот случай, что ловил Critical #2
    финального ревью (папка в разделе «остальное» — `misc-folder-topic`
    → `result/`, на диске подтверждено: исчезла из старого места,
    появилась в новом с содержимым). Проверено также: `./about.md`
    отклоняется (защита `ROOT_EDITABLE_FILES` держит альтернативное
    написание пути), перенос в `dest_dir`, который на самом деле файл —
    чистая ошибка 400, не 500.
  - Часть C: сохранение `.txt`-файла из раздела «остальное» через
    `POST /api/projects/file` — 200, содержимое реально изменилось на
    диске (раньше было бы 400).
  - Часть B, A2 (frontend): в задеплоенном `static/project-workspace.html`
    подтверждено текстом файла наличие фикса — `realPath` в
    `buildMiscTree`/`renderFolderNode` (та самая находка Critical #2),
    `expandedPaths`/`hermes-web:tree-expanded:` (Часть B). Живого браузера
    не открывали — фронтенд-логика уже дважды прослежена ревьюерами
    построчно (Task 6 review + финальное ревью), при следующем реальном
    использовании стоит один раз глазами пройти golden path.
- **Известный, сознательно отложенный минус (не блокирует, см. D-014):**
  `run.py`'s `configure_logging()` отключает `propagate` у логгера
  `aiohttp.access`, чтобы не задваивать access-строки — но на живом
  сервере подтверждено, что это **полностью глушит access-лог** в journald
  (за 5 минут активных запросов через curl — ни одной access-строки в
  `journalctl`), а не просто убирает дубли. Не влияет на саму диагностику
  A1 (отдельный логгер `hermes_web.quickchat`, подтверждена работоспособность
  выше). Стоит быстрым отдельным фиксом — убрать строку `propagate = False`
  (по мнению ревьюера, `aiohttp.web.run_app`'s собственная проверка
  `hasHandlers()` и так не даст задвоить строки).

---

**Видимые пустые папки result/ + селектор целевой папки для результата
хода (D-013) — реализовано, задеплоено на VPS, живая проверка на сервере
пройдена (2026-07-29). Готово, но живой прогон в тот же день показал:
проблема размещения файлов не решена целиком — см. пункт выше, это
продолжение той же истории.**

- Спек: `docs/superpowers/specs/2026-07-29-result-folder-targeting-design.md`.
  План: `docs/superpowers/plans/2026-07-29-result-folder-targeting.md`
  (4 задачи, TDD, Subagent-Driven Development — свежий субагент-
  исполнитель на задачу + ревью после каждой, деплой и документация
  выполнены инлайн в контроллере, а не автономным субагентом — решение
  пользователя из-за SSH-доступа к production).
- Причина: тот же живой прогон разбора Кирика отдельно вскрыл, что
  `list_tree` строил дерево `source/outer/result` только из файлов —
  пустая директория была невидима на уровне API целиком, не только в
  интерфейсе (клиентский костыль `manuallyCreatedFolders` терял её при
  перезагрузке страницы); и что модель почти всё складывала мимо
  `result/`, несмотря на общую фразу в системном сообщении.
- Реализовано: `list_tree` (`hermes_web/workspace.py`) отдаёт
  `{bucket}_dirs` для каждого bucket'а, включая пустые директории —
  костыль `manuallyCreatedFolders` убран целиком. Новый селектор «папка
  для результата» в композере чата (`static/project-workspace.html`),
  зеркалящий «папку для вложений»: выбор запоминается в `localStorage`
  по проекту, уходит в `POST /api/chat/{id}/send` как `result_target`
  (`hermes_web/app.py`), валидируется чистой строковой проверкой
  (`hermes_web/quickchat.py`) и, если валиден, усиливает системное
  сообщение конкретным абсолютным путём + просьбой спросить пользователя,
  если модель считает место неподходящим (технически заблокировать
  запись модели нельзя — согласовано с пользователем).
- 163 → 173 теста (+2/+6/+2 по задачам 1-3; Task 4 — фронтенд, без
  бэкенд-тестов). По пути ревью каждой задачи всплыло 1 Important-
  замечание (Task 3: новый тест не читал тело SSE-ответа) — исправлено
  за один круг фикса. Задеплоено на VPS (`rsync` + `systemctl --user
  restart hermes-web.service`, чистый рестарт). Живая проверка на
  сервере через реальный venv/конфиг: пустая подпапка в `result/` сразу
  видна в `result_dirs`, усиленное системное сообщение с правильным
  путём подтверждено — тестовый проект удалён после проверки. Реального
  браузера не было — фронтенд-часть проверена трассировкой диффа
  ревьюером; стоит один раз пройти golden path в браузере вживую.

---

**Автоматическая самопочинка владельца workspace-файлов (D-012) —
реализовано, задеплоено на VPS, живая приёмка пройдена (2026-07-29).
Следующий шаг — под-проект C (админ-панель).**

- Спек: `docs/superpowers/specs/2026-07-29-workspace-permissions-self-heal-design.md`.
  План: `docs/superpowers/plans/2026-07-29-workspace-permissions-self-heal.md`
  (4 задачи, TDD, выполнено инлайн в изолированном worktree).
- Причина: тот же класс бага, что и D-011, всплыл снова на следующий день
  в другом проекте — docker-песочница иногда создаёт файлы/папки от
  `root` вместо хостового uid, и починить владение такой записью может
  только root, кто бы ни владел папкой вокруг (не проблема прав `hermes`
  на свою же директорию — проблема специфична для конкретных
  root-owned записей внутри неё).
- Реализовано: новый модуль `hermes_web/permissions.py`
  (`ensure_ownership_sync`/`ensure_ownership`, никогда не бросает
  исключение), вызов перед каждым ходом чата (`quickchat.send_message`) и
  перед каждой файловой операцией UI (`workspace.save_file`/`make_dir`/
  `save_upload`); на сервере — root-скрипт
  `/usr/local/bin/hermes-fix-workspace-perms.sh` (сам перепроверяет путь
  против `/home/hermes/workspace`) + узкая sudoers-строка (единственная
  новая привилегия `hermes` сверх D-003).
- 163 теста проходят локально (было 155, +8 новых). Задеплоено на VPS
  (`visudo -c` зелёный, `rsync` + `systemctl --user restart
  hermes-web.service`). Живая приёмка: намеренно испорченный владением
  тестовый проект (`qa_temp/ALL/perm-test`) автоматически починился при
  реальном вызове `workspace.save_file`, без единого ручного `chmod`;
  `qa_temp` удалён.

---

**4 фикса живого чата (`project-workspace.html`/`workspace.py`) + 2
инфраструктурные находки живой приёмки — всё реализовано, задеплоено на
VPS и подтверждено живьём (2026-07-28).**

- Спек: `docs/superpowers/specs/2026-07-28-web-chat-live-view-fixes-design.md`.
  План: `docs/superpowers/plans/2026-07-28-web-chat-live-view-fixes.md`
  (5 задач, TDD, Subagent-Driven Development).
- **Task 1** (не рендерить пустые assistant-пузыри) — готово, ревью чистое.
- **Task 2** (`formatTime`: секунды→миллисекунды) — готово, ревью чистое.
- **Task 3** (многослотовый живой рендер хода — новый пузырь на каждый шаг,
  `recoverAfterStreamDrop()` без аргументов, полная перечитка истории) —
  готово после 1 fix-раунда: находка Important (эвристика «10с без роста
  истории → ход завершён» могла ложно сработать на паузах между шагами —
  спека документирует реальные паузы до ~100с внутри хода на 47 сообщений/
  ~17 минут) исправлена заменой на «время с момента последнего роста
  истории» (`STABLE_WINDOW_MS=120000`) с одновременным поднятием общего
  бюджета опроса с 5 до 25 минут (`MAX_WAIT_MS`) — старый бюджет уже был
  меньше задокументированного худшего случая независимо от находки.
- **Task 4** (`list_tree` показывает файлы вне `source/outer/result`,
  Python TDD, новый ключ `misc`) — готово, ревью чистое (2 второстепенные
  находки — непоследовательный пропуск скрытых файлов во вложенных
  папках и дублирование кода — учтены и закрыты финальным ревью ниже,
  см. следующий пункт).
- **Финальное ревью всей ветки** (самая мощная модель) — нашло 5 Important
  + 6 Minor; закрыто одним комплексным фикс-проходом (по правилам
  `subagent-driven-development` — второй полноценной фикс-волны не
  бывает): (1) `computeFileMessageIndex()` не унаследовал фикс Task 2
  (секунды/миллисекунды) — «↩ к сообщению» не работал после перезагрузки;
  (2) индикатор восстановления после обрыва потока стирался первым же
  опросом, чат выглядел «замёрзшим» до 2 минут без объяснения; (3) файлы
  из нового раздела `misc` с расширением `.md`/`.txt` открывались в
  редакторе, который не может их сохранить (бэкенд отклоняет запись вне
  `source/outer/result`) — теперь всегда скачиваются; (4) обход `misc` был
  ничем не ограничен по объёму (случайный `git clone`/`venv` в корне
  проекта мог заблокировать event loop) и не пропускал скрытые файлы во
  вложенных папках — добавлен потолок `MISC_MAX_ENTRIES=2000` с пометкой
  об усечении и рекурсивный пропуск скрытых путей; (5) многослотовый
  рендер Task 3 полагался на непроверенное вживую предположение «одно
  `message.started` на шаг» — добавлена независимая защита
  (`slotOpen`/закрытие слота на `assistant.completed`), работающая даже
  если предположение неверно. 2 второстепенные находки осознанно
  запаркованы (дублирование кода `_list_misc`/`list_tree` — общий хелпер
  сейчас преждевременен; символьные ссылки в корне проекта могут "утекать"
  метаданными в `misc` — без утечки данных, согласовано с решением
  предыдущего среза). Скоуп-ревью фикс-волны нашло новую регрессию
  (`recoveryStatus` не сбрасывался в начале нового хода — баннер ошибки мог
  зависать навсегда) — заказчик подтвердил точечный однострочный фикс
  сверх правила «без второй волны», проверен отдельным микро-ревью.
- **Живая приёмка (Task 5, три раунда одноразового `qa_temp`) вскрыла ещё
  2 проблемы поверх находок финального ревью — обе исправлены отдельными
  срезами до завершения приёмки:**
  1. **Task 3 на самом деле не работал для настоящего многошагового хода.**
     Живой тест (перевод 3 фраз по очереди, каждая с `write_file`) показал:
     Hermes шлёт `message.started`/`assistant.completed` **один раз на весь
     ход**, а не на каждый шаг — предположение, на которое рассчитывал весь
     Task 3 (и его защита `slotOpen` из финального ревью), оказалось
     неверным на практике. Живой вид в конце хода схлопывался в один
     пузырь с текстом финального служебного события, теряя промежуточные
     шаги — расхождение с перезагрузкой, то есть исходная жалоба заказчика
     воспроизводилась заново. **Фикс**: `sendMessage()` после чистого
     `event: done` теперь всегда один раз перечитывает полную историю
     через `GET /api/chat/{id}/messages` и заменяет ею живой вид
     (`reconcileWithHistory()`, новая функция в `project-workspace.html`)
     — то же самое, что `recoverAfterStreamDrop()` уже делает при обрыве,
     только без цикла ожидания стабильности. Многослотовый рендер по ходу
     потока остался как приблизительный «черновик», перезаписываемый
     авторитетной версией из БД. Коммит `300ec0b..df390b2`, ревью чистое,
     передеплоено, повторный живой тест подтвердил сходимость с
     перезагрузкой.
  2. **Критичная инфраструктурная находка, не связанная ни с одним из 4
     UI-фиксов**: `write_file` при `terminal.backend: docker` +
     `container_persistent: true` реально исполняется **внутри
     docker-песочницы** (подтверждено официальной документацией Hermes и
     GitHub issue [#32049](https://github.com/NousResearch/hermes-agent/issues/32049)
     про тот же класс проблемы) — `HERMES_WRITE_SAFE_ROOT` в `.env` был
     хостовым путём (`/home/hermes/workspace`), не совпадающим с реально
     смонтированным внутри контейнера `/workspace`
     (`docker_mount_cwd_to_workspace`). Файлы агента **молча терялись**:
     `write_file` рапортовал успех с правдоподобным `resolved_path`, но
     реально писал в отдельную, не смонтированную копию пути внутри
     переиспользуемого 3-дневного persistent-контейнера — хост никогда не
     получал файл. Плюс отдельно: контейнер писал файлы от `root:root`
     режимом 0600, недоступные для чтения сервисом `hermes-web` (работает
     под `hermes`), хотя в конфиге уже стоял `docker_run_as_host_user:
     true` — просто не применился к уже работающему контейнеру.
     **Фикс**: `HERMES_WRITE_SAFE_ROOT` в `.env` изменён на `/workspace`
     (сделана резервная копия `.env` в `~/.hermes/backups/` перед правкой);
     оба persistent-контейнера песочницы (`hermes-a168d268`,
     `hermes-e9b391a2`) удалены и пересозданы — новый корректно исполняется
     с `--user 1001:1001` (UID хоста `hermes`); `hermes-gateway.service`
     перезапущен. Отдельно поправлен `hermes_web/quickchat.py` (TDD,
     коммит `06c32cf`): системное сообщение раньше советовало агенту
     ОТНОСИТЕЛЬНЫЙ путь для `write_file`/`read_file` (было верно до правки
     `HERMES_WRITE_SAFE_ROOT`) — теперь советует АБСОЛЮТНЫЙ путь с
     префиксом `/workspace/...` (новая функция `_sandbox_project_path`
     конвертирует хостовый `project_path` в путь внутри песочницы). Всё
     подтверждено живьём: файл через `write_file` реально на хосте,
     владелец `hermes:hermes`, скачивается через API (200), виден в разделе
     `misc` дерева.
  - **Открытый риск на будущее, зафиксирован для чек-листа обновления
    Hermes** (см. «Обновить Hermes» в разделе «Следующие шаги»): корректность
    `HERMES_WRITE_SAFE_ROOT=/workspace` зависит от того, что Hermes и
    дальше монтирует `docker_mount_cwd_to_workspace` именно в `/workspace`
    — это код самого Hermes (открытый баг #32049 показывает, что эта
    область у них ещё не стабилизирована). При следующем `hermes update`
    — обязательно живьём перепроверить, что `write_file` всё ещё доезжает
    до хоста (не просто верить рапорту инструмента, а проверять
    `ls`/`cat` на сервере), а не полагаться на то, что конфиг «просто
    работает».
- **155 тестов проходят** (backend), финальный набор изменённых файлов
  этого среза: `hermes_web/workspace.py`, `static/project-workspace.html`,
  `tests/test_workspace.py`, `hermes_web/quickchat.py`,
  `tests/test_quickchat.py`.
- **Все три ветки слиты в `main` локально и запушены в GitHub**, все
  временные worktree/ветки-фичи удалены. Задеплоено на VPS: `rsync` каждого
  изменённого файла + `systemctl --user restart hermes-web.service`
  (дважды) и `hermes-gateway.service` (один раз, для `.env`/контейнеров).
- **Живая приёмка Task 5 пройдена полностью, одноразовым `qa_temp`**
  (заведён/удалён трижды по ходу расследования, финальный проход — один
  сквозной прогон всех 4 сценариев в одном свежем проекте): пустые пузыри
  не рендерятся; время сообщений различается и соответствует реальным
  моментам отправки; многошаговый ход (перевод 3 фраз по очереди с
  `write_file` на каждый шаг) — живой вид корректно сходится с
  перезагрузкой после `event: done`; файл вне `source/outer/result`
  появляется в разделе «🗂 остальное» дерева и скачивается. `qa_temp` и все
  его тестовые файлы/проекты удалены с сервера после проверки (включая
  один root-владением файл, оставшийся до пересоздания контейнера —
  удалён через root-доступ).
- **MVP веб-морды под-проекта B закрыт целиком, живой чат стабилен.**
  Следующий шаг — под-проект C (админ-панель, метрики VPS/wormsoft.ru).

Дизайн закрыт на уровне макетов: файловая/сессионная архитектура
([hermes-prj-structure.md](./hermes-prj-structure.md)) и дизайн веб-
интерфейса (D-009) — оба согласованы заказчиком. Макеты (кликабельный
HTML, без бэкенда): `/result/login.html`, `home.html`,
`project-selector.html`, `project-workspace.html`, `admin.html` (+ 4
черновых направления и `index.html` для истории).

**Бэкенд разбит на три под-проекта** (см. «Следующие шаги»): **A.**
Hermes-плагин `project_index` (embeddings-индекс + `project_move`) →
**B.** веб-бэкенд/API (данные Групп/Проектов, авторизация, чат) → **C.**
метрики VPS/wormsoft.ru для админ-панели. Обновление Hermes (v0.19→latest)
и per-user Matrix — по-прежнему отдельные, не начатые пункты ниже.

**Под-проект A — статус: реализован, выкачен на VPS и влит в `main`
(2026-07-26).**
Разведка на реальном сервере (подключились по SSH, свежая сверка кода
`hermes_cli/plugins.py`/`tools/registry.py`) вскрыла три расхождения с
исходным `hermes-prj-structure.md`: `numpy` на сервере не установлен
(решили — чистый Python, без него), у Hermes нет способа вызвать
инструмент напрямую в обход LLM (решили — логика в обычном импортируемом
`core.py`, регистрация тонким слоем сверху), хендлер инструмента не знает
платформенного пользователя автоматически (решили — явный параметр
`user` в схеме). Сын получает логин `rost` (Ростислав), не `gleb` — везде
далее использовать `rost`.
- Спек: [superpowers/specs/2026-07-25-project-index-plugin-design.md](./superpowers/specs/2026-07-25-project-index-plugin-design.md)
- План реализации (6 задач, TDD, с точным кодом на каждый файл, все 6
  выполнены): [superpowers/plans/2026-07-25-project-index-plugin.md](./superpowers/plans/2026-07-25-project-index-plugin.md)
- Реализация выполнена в git worktree, ветка влита в `main`. 39 тестов
  проходят и локально, и интерпретатором сервера. На VPS: плагин включён
  (`hermes plugins enable project_index`), `workspace/dem/ALL` и
  `workspace/rost/ALL` созданы, правило переиндексации — в `SOUL.md`,
  `hermes-gateway.service` перезапущен. Приёмочный тест (5 пунктов
  спека) пройден живьём через `hermes -z` — детали в
  [changelog.md](./changelog.md) за 2026-07-26. `physics-tasks/`
  мигрирован в `workspace/dem/ALL/2026-07-25_physics-tasks` с
  написанным по факту `about.md`.

**Под-проект B, срез 1+2 (авторизация + «Быстрый чат») — статус:
реализован, выкачен на VPS, приёмочный тест на реальном домене пройден
(2026-07-26).**
- Спек: [superpowers/specs/2026-07-26-web-backend-auth-chat-design.md](./superpowers/specs/2026-07-26-web-backend-auth-chat-design.md)
- План реализации (9 задач, TDD, Subagent-Driven Development, все 9
  выполнены с ревью на каждую задачу): [superpowers/plans/2026-07-26-web-backend-auth-chat.md](./superpowers/plans/2026-07-26-web-backend-auth-chat.md)
- Пакет `hermes-web/` (Python/aiohttp): SQLite-хранилище (users/
  web_sessions/chat_sessions), argon2-аутентификация с куки-сессиями и
  rate limit'ом, клиент Hermes API server (SSE), «Быстрый чат» поверх
  уже задеплоенного `project_index` (создаёт проект в `ALL`, индексирует
  его напрямую импортом `core.py`, без похода через LLM), веб-приложение
  на aiohttp, сид-скрипт пользователей без само-регистрации, фронтенд
  (доработанные макеты `login.html`/`home.html` + новый `chat.html`).
  45 тестов проходят и локально, и интерпретатором сервера (35 из
  реализации + 10 из финального ревью, см. ниже).
- Выкатили на VPS: включили платформу Hermes `api_server`, подняли
  `hermes-web.service` (systemd, `127.0.0.1:8643`), засеяли `dem`
  (owner) и `rost` (participant) со сгенерированными паролями, поставили
  Caddy с автоматическим TLS на `hermes.blackboxbegin.space` →
  `127.0.0.1:8643`, включили `ufw` (только 22/80/443).
- Приёмочный тест на реальном домене пройден: вход, создание проекта
  «Быстрого чата», реальный стриминг ответа агента по SSE.
- **Финальное ревью всей ветки (13 коммитов)** нашло 1 критичную (корневой
  `/` отдавал 403 вместо страницы входа — ровно то, что видят реальные
  пользователи) и 6 важных проблем (обработка сбоев Hermes в SSE-потоке,
  таймаут HTTP-клиента, блокировка event loop на ~44с при создании чата,
  неограниченная память у rate limiter, отсутствие теста на
  межпользовательский доступ, «осиротевшие» проекты при сбое Hermes) —
  все реально воспроизведены на живом сайте. Один фикс-проход закрыл все
  7, scoped re-review подтвердил чистоту. Детали —
  [changelog.md](./changelog.md) за 2026-07-26 (запись «финальное ревью»).
- **Статус: ветка влита в `main` (git merge, локально), запушена в
  GitHub, передеплоена на VPS.** `git worktree`/ветка-фича удалены.

**Под-проект B, срез 3, часть 1 (навигация по Группам/Проектам) — статус:
реализован, выкачен на VPS, приёмочный тест на реальном домене пройден
(2026-07-26).**
- Спек: [superpowers/specs/2026-07-26-web-projects-navigation-design.md](./superpowers/specs/2026-07-26-web-projects-navigation-design.md)
- План реализации (6 задач, TDD, Subagent-Driven Development, все 6
  выполнены с ревью на каждую задачу): [superpowers/plans/2026-07-26-web-projects-navigation.md](./superpowers/plans/2026-07-26-web-projects-navigation.md)
- Расширили уже задеплоенный плагин `project_index`: колонка `description`
  в индексе, `updated_at` теперь = mtime `about.md` (а не время вызова
  индексации), новый быстрый SQL-листинг с фильтрацией по времени
  активности (`week`/`month`/`quarter`/`half_year`/`year`/`all`) и статусу —
  рассчитан на масштаб до ~1000 проектов в год без обхода диска.
- Новая таблица `group_meta` в `hermes-web.db` (эмодзи/имя/закрепление
  группы — независимо от имени папки на диске), новый модуль
  `hermes_web/projects.py`, 7 новых эндпоинтов (`/api/groups`,
  `/api/projects`, `/api/projects/detail`, `/api/projects/search`,
  `/api/projects/move`), экран «Работа с проектами»
  (`project-selector.html`) на реальном API — список/поиск по смыслу
  (RAG)/фильтры/создание и редактирование групп/перенос проектов между
  группами. Полноценный `project-workspace.html` — сознательно вне
  рамок, отдельный следующий срез.
- 149 тестов проходят и локально, и интерпретатором сервера (64 в
  `project_index`, 85 в `hermes-web`).
- **Финальное ревью всей ветки (7 коммитов)** нашло 1 критичную и 3
  важные проблемы, все реально проверены на живом коде/сервере:
  - **Критично**: перенос проекта между группами (`move_project` в
    `project_index`, впервые в этом срезе ставший прямым HTTP-эндпоинтом,
    раньше был доступен только через инструмент агента) не проверял
    целевой путь на выход за пределы папки пользователя — запрос вида
    `new_group: "../rost"` реально перемещал проект в чужое пространство
    (или вовсе за пределы workspace) ещё ДО того, как функция бросала
    ошибку «не найдено», то есть API рапортовал о неудаче, а перенос уже
    произошёл. Проверено эксплуатацией на живом сервере (безопасно, тем
    же вечером, сразу же исправлено) — межпользовательской утечки не
    произошло, аудит `dem`/`rost` на сервере после фикса чист.
  - Сырые embedding-векторы (1024 float на проект) утекали в каждый ответ
    `/api/projects`/`/api/projects/search` — при целевом масштабе (~1000
    проектов) это ~20 МБ и больше секунды синхронной сериализации на
    однопоточном event loop; заодно чинит попутную проблему — те же
    векторы раньше улетали в контекст LLM при `project_search_similar`
    (~30k лишних токенов на вызов).
  - Отсутствие теста на межпользовательский доступ и на перенос-эксплойт
    на уровне HTTP-маршрутов; два async-теста, не проверявших реальное
    использование `run_in_executor`.
  - Один фикс-проход закрыл все 4, scoped re-review подтвердил чистоту
    (включая независимую проверку эксплойта на исправленном коде).
    Исправленный код передеплоен на VPS и проверен живьём (перенос с
    `new_group: "../rost"` теперь корректно отклоняется, `embedding`
    больше не в ответах).
- **Статус: ветка влита в `main` (git merge, локально), запушена в
  GitHub, передеплоена на VPS.** `git worktree`/ветка-фича удалены.

**Под-проект B, срез 3, часть 2 (`project-workspace.html`) — статус:
реализован, выкачен на VPS, приёмочный тест на реальном домене пройден
(2026-07-28).**
- Спек: [superpowers/specs/2026-07-27-web-project-workspace-design.md](./superpowers/specs/2026-07-27-web-project-workspace-design.md)
- План реализации (10 задач, TDD, Subagent-Driven Development, все 10
  выполнены с ревью на каждую задачу): [superpowers/plans/2026-07-27-web-project-workspace.md](./superpowers/plans/2026-07-27-web-project-workspace.md)
- Новый модуль `hermes_web/workspace.py` — трёхслойная защита путей
  поверх уже проверенного `project_index_core.resolve_project_path`
  (проект принадлежит пользователю → файл не выходит за пределы проекта
  → для mkdir/upload/сохранения файл лежит внутри `source`/`outer`/
  `result`), дерево файлов с поддержкой произвольной вложенности
  подпапок (D-010), чтение/сохранение `.md`/`.txt`, создание подпапок,
  загрузка вложений с дата-префиксом (409 на коллизию имени, без тихого
  авто-суффикса — та же политика, что и в `move_project`).
- `quickchat.get_or_open_session` — ленивая Hermes-сессия на уже
  существующий проект (создаётся при первом открытии, переиспользуется
  дальше), без утечки сессии при сбое Hermes.
- Сохранение корневого `about.md` асинхронно триггерит переиндексацию
  (`run_in_executor`, не блокируя event loop — тот же приём, что уже
  закрыл аналогичный риск в срезе 1+2).
- 6 новых HTTP-эндпоинтов (`/api/projects/open`, `/tree`, `/file` GET/
  POST, `/mkdir`, `/upload`), `project-selector.html` получил кнопку
  «Открыть проект →», `project-workspace.html` подключён к реальному API
  (дерево, чат с рендером Markdown и копированием исходника, «Активность
  агента» — эфемерно поверх уже идущего SSE, без новых таблиц).
- 199 тестов проходят локально (64 в `project_index`, 135 в
  `hermes-web`).
- Ход разработки прервался компактированием контекста в середине фикса
  находок Task 8 (XSS-экранирование в рендере Markdown, отсутствующие
  401-редиректы на 4 fetch-запросах, видимость пустых папок, ложное
  срабатывание маркера вложений) — по возвращении фикс был независимо
  проверен как уже примененный, докоммичен и подтверждён.
- **Первый приёмочный тест на реальном домене** (одноразовый временный
  аккаунт `qa_temp`, удалён после теста, реальные `dem`/`rost` не
  трогали): переоткрытие проекта переиспользует ту же Hermes-сессию;
  создание подпапки в `source` по фамилии автора (реальный сценарий
  заказчика из брейнсторма) и загрузка в неё файла работают через API и
  через живой чат — агент реально прочитал загруженный файл по вложению
  в сообщении; SSE `tool.started`/`tool.completed` стримятся живьём;
  сохранение `about.md` триггерит переиндексацию; история чата переживает
  перезагрузку; межпользовательский доступ и path traversal на GET-файле
  корректно отклоняются 404.
- **Финальное ревью всей ветки (11 коммитов, самая мощная модель) нашло
  1 критичную и 6 важных проблем** — сильно отличается от предыдущих
  срезов тем, что каждая мандатная задача плана прошла свою же
  ревью чисто (весь код и тесты — почти дословно то, что было прописано
  в плане), а находки вскрылись только на разборе всей ветки целиком:
  - **Критично**: `workspace.save_upload` проверял коллизию имени через
    `os.path.exists`/писал через `open(..., "wb")` — оба следуют по
    симлинку. Если положить в `source`/`outer`/`result` битую симлинку
    с именем будущей загрузки, указывающую наружу проекта (агент может
    создать такую симлинку сам, будучи всё ещё внутри своей Docker-клетки,
    D-004), следующая загрузка с этим именем реально писала контент
    **за пределами** проекта и sandbox write-safe-root — то есть
    превращала заблокированную запись агента в незаблокированную запись
    на хосте. Уже была на проде на момент находки. Тот же класс бага
    (путь наружу, находимый только исполнением, не чтением кода), что и
    закрытый ранее traversal в `move_project`.
  - 6 важных: загрузка молча резалась на 1 МиБ (реальный кейс — сканы
    учебников, почти всегда больше); `Content-Disposition` собирался
    строкой напрямую — `"` в имени файла ломал заголовок, `\r`/`\n`
    рвал соединение, кириллица в именах (`Иванов`) уходила без RFC 6266
    `filename*=`; слой 1 проверял только «путь внутри пространства
    пользователя», не «это вообще проект» — `mkdir`/`tree` реально
    работали на голом корне пользователя/группы, создавая
    псевдо-группы/псевдо-проекты в списках; чтение файла на отдачу
    целиком грузило файл в память синхронно, блокируя event loop для
    всех пользователей на файлах агента без ограничения размера
    (`result`/`outer`); `mtime` отдавался без UTC-суффикса — на фронте
    парсился как локальное время, сдвигая отображаемое время файлов и
    ломая сопоставление с временем сообщений в чате; ни одного теста на
    межпользовательский доступ к трём пишущим эндпоинтам (`file` POST/
    `mkdir`/`upload`) — поведение было верным по факту, но без защиты от
    регрессии.
  - Один фикс-проход закрыл все 7 (коммит `7830f3b`, +9 тестов → 208),
    scoped re-review подтвердил все ADDRESSED без новых критичных/важных
    регрессий (1 некритичный комментарий в коде запаркован). Исправленный
    код немедленно передеплоен на VPS (симлинк-баг был живым в проде) и
    проверен живьём ещё раз: `mtime` с `+00:00`, RFC 6266 `filename*=` с
    кириллицей, голый корень пользователя/группы теперь 404 — тем же
    одноразовым `qa_temp`, удалённым после проверки.
  - ~12 второстепенных находок (TOCTOU в `make_dir`, символьные ссылки в
    `list_tree`, отсутствие сворачивания папок в UI и др.) осознанно
    запаркованы как неблокирующие — детали в отчёте финального ревью,
    не переносились в этот срез.
- **Статус: ветка влита в `main` (git merge, локально), запушена в
  GitHub, передеплоена на VPS.** `git worktree`/ветка-фича удалены.
- **MVP веб-морды под-проекта B закрыт целиком.** Следующий шаг —
  под-проект C (админ-панель, метрики VPS/wormsoft.ru).

**Первый живой прогон реального сценария (физика/кинематика, PDF-задачник
Кирика) — 2 бага найдены и исправлены (2026-07-28).**
- Заказчик попросил агента решить задачу по загруженному PDF через рабочий
  экран проекта. Ход завис — по логам сервера восстановлена причина:
  - **Путаница с путями**: внутри Docker-песочницы агента (D-004) путь
    `/workspace/...` — алиас на `/home/hermes/workspace/...` на хосте, но
    этот алиас понимает только shell внутри `terminal`/`execute_code`.
    Инструменты `write_file`/`read_file` — отдельные, не-докеризованные,
    и сверяют путь с `HERMES_WRITE_SAFE_ROOT` напрямую как путь хоста.
    Абсолютный `/workspace/...` в `write_file` поэтому ВСЕГДА получает
    отказ («outside HERMES_WRITE_SAFE_ROOT»), даже когда тот же путь
    относительно cwd сработал бы. Хуже: модель отрапортовала пользователю
    «готово, файл сохранён» вопреки явной ошибке инструмента — файла не
    существовало на диске (проверено `find` на сервере). Это код
    `agent/file_safety.py` — вендорный, не наш, править нельзя (потеряется
    при `hermes update`) — фикс только промптом.
  - **SSE обрывался на долгих тихих паузах** (докер, ожидание wormsoft.ru
    — сама задача заняла ~8 минут) — прокси/браузер рвали простаивающее
    соединение раньше, чем агент реально заканчивал. Бэкенд продолжал
    работать и дописывал ответ в БД, но фронтенд не переподключался и не
    переспрашивал историю — ответ был виден только после ручной
    перезагрузки страницы.
- **Фикс** (без формального плана — прямая работа в этой сессии, TDD на
  Python-части): `hermes_web/quickchat.py` — system-сообщение на каждый
  ход теперь явно требует относительные пути для `write_file`/`read_file`,
  готовое класть в `result/`, и не рапортовать об успехе без проверки
  ответа инструмента; `hermes_web/app.py` — heartbeat (`: ping`) каждые
  15с во время тихих пауз потока (`asyncio.wait` над одной и той же
  pending-задачей, не `asyncio.wait_for` — таймаут не должен отменять
  реальный запрос к Hermes); `chat.html`/`project-workspace.html` — при
  обрыве потока (исключение или конец без `event: done`) переспрашивают
  историю чата вместо пустого/повисшего пузыря.
- **Ревью нашло 2 критичные проблемы в первом фикс-коммите**, обе
  исправлены отдельным коммитом: утечка фоновой задачи в `app.py` (следующий
  `__anext__()` планировался ДО записи текущего payload — при обрыве записи
  уже запущенная фоновая задача всё равно доводила генератор, никем не
  дожидаемая) и неверная логика восстановления в `chat.html` (брала
  последнее по времени сообщение ассистента во всей истории, а не ответ
  именно на этот ход — путала ответы при двух подряд отправленных
  сообщениях). Плюс защита от двойной отправки в `project-workspace.html`.
  213 тестов проходят (было 208, +5 новых). Выкачено на VPS, живой
  смоук-тест пройден.
- **Открытый вопрос, не в рамках этого фикса**: `approvals.mode: manual`
  (D-004) в gateway-контексте (веб-чат, не интерактивный CLI) не имеет
  штатного способа для обычного пользователя дать подтверждение —
  Hermes поддерживает команду `/approve` в чате для этого, но нигде не
  задокументировано и не подсказывается пользователю. В живом инциденте
  агент упёрся в `pending_approval` на попытке запустить скрипт извлечения
  страниц PDF и не смог продолжить этим путём (в итоге обошёл проблему
  сам, другим способом). Нужно решение: либо научить пользователей
  `/approve` (проще, ничего не меняя в конфиге), либо пересмотреть
  `approvals.mode` для веб-контекста — см. «Открытые вопросы» ниже.

**Второй живой прогон в тот же день (2026-07-28) — ещё 4 бага, диагностика
завершена, спек написан, реализация не начата.**
- Заказчик прогнал более длинный сценарий (перевод задач 3.23–3.29 из
  скана учебника Кирика) через рабочий экран проекта и прислал скриншоты
  4 проблем. Диагностика — чтением сырых данных истории напрямую из
  Hermes API (`GET /api/sessions/{id}/messages` на 8642), не только
  логов.
- **Найдено и объяснено**: (1) пустые пузыри в чате — легитимные пустые
  `assistant`-сообщения (модель молча вызвала инструмент без
  комментария), рендерятся как пустые бабблы; (2) одинаковое время у
  всех сообщений — Hermes отдаёт секунды, `formatTime()` кормит их в
  `new Date()` как миллисекунды, разница схлопывается до долей секунды;
  (3) чат не обновляется сам на многошаговых ходах — один вопрос
  пользователя может породить **десятки отдельных сообщений ассистента**
  (в этом случае 47), а наш живой чат считает, что на один вопрос — один
  ответный пузырь, и просто перезаписывает один и тот же слот; обрыв SSE
  (уже чинен раньше сегодня) — не главная причина здесь; (4) итоговый
  файл существует на диске в этот раз (в отличие от утреннего инцидента),
  но лежит в произвольной папке в корне проекта, а не в `result/` —
  system-сообщение про `result/` снижает частоту, но не убирает
  полностью; дерево файлов в UI показывает только `source/outer/result`,
  произвольные папки не видны.
- Спек:
  [superpowers/specs/2026-07-28-web-chat-live-view-fixes-design.md](./superpowers/specs/2026-07-28-web-chat-live-view-fixes-design.md) —
  для каждого из 4 багов: точная причина, конкретный фикс, куда именно в
  коде. **Реализация — следующий шаг.**

## Следующие шаги

- [x] **Архитектура памяти/файлов сессий Hermes — дизайн готов (2026-07-25).**
  Полный дизайн + техническая часть embeddings-индекса (2026-07-25, доп.):
  [hermes-prj-structure.md](./hermes-prj-structure.md) — новый плагин
  по образцу `content-injection-guard` (`ctx.register_tool`), SQLite +
  brute-force cosine (не `sqlite-vec`, не внешняя vector DB), модель
  `qwen/qwen3-embedding:8b` (D-001). Плюс перенос проекта между `ALL`/
  группами (`project_move`, тот же плагин — единая точка логики для
  агента и будущего веб-бэкенда, дата-префикс `ALL` туда/обратно,
  пересчёт индекса всегда). Реализация (сам плагин) не начата.
  Сверились с официальной документацией Hermes — `project_create`/
  `project_list`/`project_switch` не существуют (это фича Desktop-сайдбара
  про git-репозитории, не про темы), встроенные `memory`/`session_search`
  ничего из задачи не закрывают (плоские глобальные файлы / keyword FTS,
  не смысловой поиск) — дублирования со штатным Hermes нет. Реализация
  (сам embeddings-индекс, миграция текущей рабочей папки под новую
  структуру) — не начата, отдельный следующий шаг.
  Исходная проблема от заказчика (2026-07-24), для истории: каждый диалог
  с Hermes — это сессия,
  внутри неё идёт обмен:
  - **Вход**: текстовые запросы + материалы — скриншоты, видео, аудио,
    ссылки на сайты.
  - **Обработка**: агент своими инструментами (LLM + tools) превращает
    материалы в машиночитаемый текст, передаёт между разными LLM
    (вижн → текстовая модель и т.п., см. D-001 про роутинг).
  - **Выход**: текстовый ответ ИЛИ файлы — заказчик оценивает, что
    **в 50%+ случаев на выходе будут именно файлы** (`.md`, Word, Excel,
    PDF, DXF и т.п.), не просто текст в чате.
  - **Требование**: всё это надо хранить так, чтобы через месяц или год,
    среди десятков тысяч таких сессий, можно было найти нужное. Минимум —
    группировка по темам. Идея заказчика — вести индекс в двух видах
    одновременно: человекочитаемый (Markdown, по духу похоже на Obsidian/
    Zettelkasten) и машиночитаемый для нейронки (RAG/embeddings-массив
    для быстрого поиска по смыслу, не по ключевым словам).
  - **Что делать завтра**: сначала посмотреть, что уже умеет сам Hermes
    из коробки (официальная документация/репозиторий — правило CLAUDE.md):
    встроенная память (`memory` в config.yaml, инструмент `memory`),
    полнотекстовый поиск по сессиям (`hermes sessions`/SQLite FTS,
    упоминалось при настройке дашборда), `project_create`/`project_list`/
    `project_switch` (видели в списке инструментов агента, не изучали
    подробно) — возможно, часть задачи уже решена штатно и не нужно
    городить своё. Отдельно поискать в интернете/на GitHub: готовые
    паттерны PKM-подобной памяти для AI-агентов (Obsidian-vault-as-memory,
    RAG поверх заметок, аналоги для других агентных фреймворков) — для
    идей, не для слепого копирования (проект решил не копировать гайды
    бездумно, см. CLAUDE.md про Futura AI).
  - Файловая часть тесно связана с уже сделанным сегодня объединением
    рабочей папки (`/workspace` = write-safe-root, см. D-004 доп.) —
    вероятно, структура подпапок «по задаче» должна эволюционировать в
    структуру «по теме/проекту» с индексом, а не просто разово копиться.

- [x] **Отдельная веб-морда для общения с Hermes — срез 1+2 (авторизация +
  «Быстрый чат») реализован и выкачен (2026-07-26).** Решение от
  заказчика (2026-07-25): отказ от общения с Hermes через Matrix,
  переход на отдельный веб-интерфейс на своём доменном имени. Заменяет
  Matrix-шлюз как основной канал доступа (был требованием #2 брифа —
  см. [project-brief.md](./project-brief.md)); Matrix-инфраструктура
  (D-005/D-007) технически не сносится, но перестаёт быть приоритетом.
  Реализовано (см. «Сейчас в работе» выше): вход по логину/паролю на
  `hermes.blackboxbegin.space`, «Быстрый чат» с Hermes через браузер.
  **Срез 3, часть 1 (навигация — `project-selector.html`) и часть 2
  (полноценный `project-workspace.html`) реализованы и выкачены
  (2026-07-26 и 2026-07-28 соответственно, см. выше).** MVP веб-морды
  закрыт целиком. Остаётся отдельным пунктом: админ-панель
  (под-проект C) — не начата.
- [ ] **Полноценное приложение под Android.** Решение от заказчика
  (2026-07-25), следующий шаг после веб-морды. Детали не проработаны.

- [ ] **Разобрать `source/2nd brief Olyansky.md` (добавлено 2026-07-26).**
  Пост опытного админа/разработчика по нейросетям (Иван Олянский) про
  связку Hermes + GPT 5.6 Sol — сценарии использования (архиватор
  документов, глубокий исследователь, документ-ассистент, email-маркетинг
  помощник; предупреждение — плохо подходит для сложной внутренней
  разработки) и критичные настройки (auto-restart крон каждые 15 минут
  для зависших задач, чёткие правила для браузера, структурированные
  топики в боте, отдельная почта для агента, работа строго в Docker,
  минимально необходимые доступы). Задача — разобраться, что из этого уже
  закрыто нашей архитектурой (Docker-клетка — D-004, роутинг моделей —
  D-001, `content-injection-guard`), что применимо к нашему кейсу, а что
  нет, и не стоит ли добавить новый функционал по мотивам (например,
  auto-restart зависших задач гейтвея). Не копировать бездумно — сверять
  с официальной документацией Hermes при любом расхождении (правило
  CLAUDE.md про `source/Futura AI/`, то же самое применимо и здесь).

- [ ] **Обновить Hermes.** Дашборд показывает «v0.19.0 — update available» —
  проверить, какая версия реально последняя (сверить с релизами в
  https://github.com/NousResearch/hermes-agent, официальный источник, не
  гадать по дашборду) и обновиться. **Не сломав** сегодняшние настройки:
  - `config.yaml` — версия схемы `0` при актуальной `33` (уже отложенный
    риск, `hermes doctor --fix`/`config set` вычищают комментарии, см.
    ниже отдельный пункт — вероятно, обновление версии Hermes и миграция
    config schema теперь одна и та же задача, а не два разных дела).
  - Обходы бриджинга через `.env` (`TERMINAL_CWD`,
    `TERMINAL_DOCKER_MOUNT_CWD_TO_WORKSPACE`, ранее `TERMINAL_DOCKER_VOLUMES`) —
    проверить, не начнёт ли новая версия читать эти ключи из `config.yaml`
    штатно (тогда `.env`-дубли можно будет убрать) и не сломает ли
    наоборот что-то, что сейчас работает через `.env`.
  - Наш плагин `content-injection-guard`, правки `SOUL.md` (только что
    вылеченный баг с блокировкой антиинъекционным сканером — см. D-004),
    роутинг моделей (D-001), Matrix-настройки (D-005/D-007) — всё это
    правки поверх файлов Hermes, при апдейте могут откатиться/
    конфликтовать.
  - Перед обновлением — сделать копию `~/.hermes/config.yaml`, `.env`,
    `SOUL.md`, `plugins/content-injection-guard/` (например, tar в
    `~/.hermes/backups/`), обновляться командой `hermes update`, после —
    прогнать `hermes doctor` и сверить, что все наши кастомизации
    пережили обновление (заново тестами из этого дня: живой ответ в
    Matrix, `vision_analyze`, доставка файла вложением, `/bg`).
  - **Новое (2026-07-28): живьём перепроверить, что `write_file` реально
    доезжает до хоста** — не верить рапорту инструмента (`bytes_written`,
    `resolved_path`), а проверить `ls`/`cat` на сервере после тестовой
    записи. `HERMES_WRITE_SAFE_ROOT=/workspace` (было `/home/hermes/workspace`)
    и пересозданные с `docker_run_as_host_user` контейнеры песочницы —
    подстройка под текущее поведение docker-бэкенда Hermes при
    `terminal.backend: docker`; если апдейт поменяет, как монтируется
    `docker_mount_cwd_to_workspace`, эта настройка может снова разойтись с
    реальностью (см. открытый баг Hermes про смежный класс проблемы:
    https://github.com/NousResearch/hermes-agent/issues/32049). Детали
    находки и фикса — запись в `changelog.md` за 2026-07-28.

- [x] Согласовать и реализовать архитектуру sandbox/permissions против prompt injection (D-004)
- [x] Установить Docker на VPS, добавить `hermes` в группу `docker`
- [x] Установить Hermes (terminal.backend docker)
- [x] Подключить Matrix gateway, проверить подключение (D-005)
- [x] Получить API-ключ wormsoft.ru и прописать роутинг моделей в config.yaml (см. D-001)
- [x] Подключить веб-поиск (ddgs, бесплатно) — см. D-006
- [x] Поднять `hermes gateway` постоянно (systemd, linger включён)
- [x] Первый сквозной диалог в Matrix — подтверждён заказчиком (в незашифрованной «Hermes room», D-007)
- [x] Веб-дашборд Hermes поднят постоянно (systemd, `127.0.0.1:9119`), доступ заказчика через SSH-туннель подтверждён скриншотом
- [x] Доустановить browser-tools (`npm install` под `hermes` с верным PATH) — агент теперь реально заходит на страницы через `browser_navigate`
- [x] ~~Perplexity~~ — не понадобилась: поиск (`ddgs`) + browser-tools закрывают задачу «искать и заходить на любую страницу» бесплатно (D-006 доп.)
- [ ] Кастомный DXF-скилл (нет готового в каталоге Hermes)
- [x] Единая рабочая папка агента: `/workspace` внутри Docker-песочницы = `/home/hermes/workspace` на хосте = write-safe-root = дашборд FILES = доставка вложений (через `.env`, `config.yaml` не бриджится). Правило про подпапки на задачу — в SOUL.md. См. D-004.
- [x] **Критично почищено**: SOUL.md блокировался антиинъекционным сканером Hermes с самого начала (наш же пример «ignore previous instructions» триггерил фильтр) — все правила безопасности из SOUL.md не доходили до модели весь день. Переписано без триггер-фраз, проверено сканером, подтверждено живым вызовом. См. D-004.
- [ ] ~~Решить, мигрировать ли config.yaml (v0 → v33)~~ — объединено с пунктом «Обновить Hermes» выше, это теперь одна задача
- [ ] Опционально: включить E2EE для Matrix (нужен `libolm-dev` системно под root; `python-olm` уже стоит) — не запрошено, отложено

## Открытые вопросы / блокеры

- **`approvals.mode: manual` (D-004) и веб-чат/gateway-контекст без
  подсказки про `/approve`** (найдено 2026-07-28). Ручное подтверждение
  опасных команд задумано как защита от prompt injection, но в
  gateway-режиме (Matrix/веб, не интерактивный `hermes chat`) пользователь
  никак не узнаёт, что нужно ответить `/approve` — агент просто получает
  `pending_approval` и должен как-то выкручиваться сам. Решить: либо
  задокументировать `/approve` для пользователей (`docs/manual.md`),
  либо пересмотреть `approvals.mode` конкретно для веб/gateway-сессий.

## Карта проекта (опорные точки)

### VPS
- IP: `212.115.55.116`, Ubuntu 24.04.4 LTS
- Железо: 8 vCPU / 8GB RAM / 89GB NVMe (RU-датацентр) — см. D-002
- Провайдер: панель на vmmanager (см. скриншот тарифа в истории чата);
  название хостера уточнить и вписать сюда при следующем шаге

### Аккаунты на сервере
| Аккаунт | Доступ | Права | Назначение |
|---|---|---|---|
| `root` | SSH-ключ (локально: `~/.ssh/id_ed25519_hermes_vps`) | полные | администрирование/установка ПО |
| `deploy` | создана заказчиком заранее | состоит в группе `sudo`, пароль неизвестен нам | резервный админ-доступ на случай отключения root-логина по SSH; пока не используем |
| `hermes` | SSH-ключ (локально: `~/.ssh/id_ed25519_hermes_user`) | без sudo, пароль заблокирован (`passwd -l`); группа `docker` — добавить после установки Docker | под этим аккаунтом будет крутиться сам агент |

Приватные ключи живут только локально в `~/.ssh` на машине-администраторе,
в git не попадают (см. `.gitignore` и правило в `CLAUDE.md`). Паролей от
серверных аккаунтов в текстовом виде нигде нет — если понадобится реальный
пароль (например, для `deploy`), хранить в менеджере паролей, а не в репо.

### Matrix
- Homeserver: `https://matrix.ctvuprising.ru` (домашний сервер заказчика)
- Аккаунт бота: `@hermes:matrix.ctvuprising.ru`, доступ по access token (D-005)
- Allowlist (`MATRIX_ALLOWED_USERS`): `@dem:matrix.ctvuprising.ru` (заказчик), `@glapetram:matrix.ctvuprising.ru` (сын)
- Токен и всё остальное — только в `~/.hermes/.env` на сервере, не в репозитории

### Hermes на сервере
- Установлен под `hermes`: `~/.hermes/` (config.yaml, .env, SOUL.md), код — `~/.hermes/hermes-agent/`
- Наш плагин «клетки»: `~/.hermes/plugins/content-injection-guard/`
- `tirith` бинарник: `~/.local/bin/tirith` (поставлен вручную с GitHub Releases, не через brew)
- Рабочая папка агента (write-safe-root): `/home/hermes/workspace`
- Веб-дашборд: `hermes-dashboard.service` (systemd user-юнит, `~/.config/systemd/user/`),
  всегда `127.0.0.1:9119` на сервере. Открыть у себя в браузере:
  `ssh -L 9119:127.0.0.1:9119 root@212.115.55.116` (PowerShell/терминал, держать
  окно открытым), затем `http://127.0.0.1:9119`

### Документы
- Бриф: [project-brief.md](./project-brief.md)
- Инструкция пользователю (как работать с живым агентом): [manual.md](./manual.md)
- Решения: [decisions.md](./decisions.md) (D-001 роутинг моделей, D-002 VPS, D-003 аккаунт hermes, D-004 клетка)
- Справочник по API wormsoft.ru (матрица ошибок, лимиты): [wormsoft-api.md](./wormsoft-api.md)
- Сырые материалы: `source/1st brief.md`, `source/Futura AI/`


